# Schema-11 Claude unconditional-route final diagnostic completed — 2026-08-18

The wholly fresh successor is frozen at:

`/private/tmp/engram-native-memory-pilot-v1-claude-unconditional-route-sealed.g4yXMZ/run-plan.json`

Prepared run-plan SHA-256:

`16ba5152463ac9aa370d5fae573878482d8d4405d47e0d850c8bd7a79bce91ef`

It contains six fresh Claude Code lanes: matched and mismatch source-backed cases across native
memory, lean Engram, and combined memory. It has no Codex lanes, authentication homes, or activation
phase. It never reuses a completed or invalid lane path.

The candidate contract makes one bounded repository-local `procedure_match` mandatory after
orientation for every actionable repository task, using the full exact request. It explicitly
includes requests to handle an operation or use durable procedure memory and says
`memory(action=list)` is not a substitute. Project ambiguity blocks project/task memory only.

This is a single-host, one-repetition diagnostic. It can validate or reject the Claude route, but
cannot establish portability or a statistically powered product claim.

## Frozen identities and gates

- Protocol:
  `evals/native_memory_pilot_v1/protocol-schema-11-claude-unconditional-route-final-diagnostic.json`,
  SHA-256 `9b621ff8ee656aa87cad666722dea4d8b51da4aec6b0acee86f5ee1ed4b94bdb`.
- Engram: `1bde61b2d999ec55a68e6b3cd9a09be8e4aa23c56bd5bddebe07b8ea9322a322`.
- Evaluator: `546ba85b8b0bec5d9c19d29de5001017d2371f1092d7a7787b69698a247d3879`.
- Claude Code 2.1.234:
  `08d8700313697cbe730a25420c908a299ce52d56f0eb2cf4fac94cab5109bc57`.
- Codex 0.148.0-alpha.9, attested but unused:
  `6170ff5578170ee9b74ad92bfcff96e6186f41d02b60815a7c2b01ad424c754f`.
- Generated Claude adapter:
  `82b51fb0a093021b309b0c2faf5604597615e09e250e983a92c5dfd23d5798c1`.
- Effective six-tool schema:
  `b704fac7da773e703dfca6bd3e9b0ffa6dcade6b259e7290315d969de3cec5b3`.
- Effective agent instructions:
  `09be365fafaf2542d137eb69384b1db1b1ac2166c1d6a71a6995cf84a27c3eac`.

The exact binaries were copied byte-for-byte to a compact immutable directory before their 24.6
GiB disposable Cargo target was cleaned. Their hashes are unchanged. Runtime attestation is
`verified=true`, including matching effective MCP hashes and successful restricted-tool and
review-authority rejection probes. Authentication readiness is `ready=true` with an empty Codex
lane list. The untouched audit is `invalid=false`, with six prepared lanes, no failures or writes,
and every provider trace pending.

The runner now applies Engram's own persistent-store headroom policy before the first provider
process. At final preparation the filesystem had 47,693,946,880 bytes available against a required
19,893,251,686-byte reserve. An isolated start/stop probe using the same Engram candidate succeeds.
The preflight regression and all 115 evaluator tests pass; evaluator Clippy, formatting, and diff
checks pass. The routing change also passes 271 index tests (one model-download test ignored), its
doc test, all 57 CLI tests, affected Clippy, and formatting. Repository `target/debug` is absent.

## Budget and authority boundary

Prior accounted Claude spend is 2,723,115 micro-USD, including the invalid predecessor's one
33,232-micro-USD call. This plan allocates at most $1.00 and retains the $4.00 cumulative ceiling.
The 12 teaching/evaluation calls each receive a $0.083 turn-boundary allocation. Standing provider
authorization satisfies the execution gate without weakening exact budget, hash, lifecycle, or
outcome checks.

No live adapter, settings, hook, skill, daemon, or harness installation is authorized or performed.

## Completed lifecycle and immutable outputs

Teaching ran exactly once from `2026-08-18T16:35:25+03:00` to
`2026-08-18T16:38:25+03:00`; evaluation ran exactly once from
`2026-08-18T16:41:13+03:00` to `2026-08-18T16:43:30+03:00`. Every provider
call exited 0, with no recovery, replay, accepted budget exit, integrity failure, or native-memory
shell write attempt. All four Engram teaching candidates passed the evaluator's exact trusted
verification before evaluation.

The sealed artifacts are:

- teaching runner:
  `9b967603ef78b5180e676a3759435e294da2267f3385f64cf81e6ca3df18e3d3`;
- evaluation runner:
  `d07898eb7fc4d1d155e4acedb0f9321032e1a2edb5447346e39f59c906a7496c`;
- completion audit:
  `9dcf8a9f1fe9e682b03fddcb89347a5fd607f68c83a8f51c9e4e3f635bf535cb`;
- comparison report:
  `f44e66da38b66238dc4dc1d8e7c9df60e232221815ff3fe54d951b5ca15d5e10`;
- strict comparison report: the same JSON and SHA-256; the strict command exits 1 because a
  Claude-only diagnostic cannot establish portable value across hosts.

The final audit is `complete=true`, `invalid=false`, and
`all_acceptance_passed=false`. Teaching cost 232,490 micro-USD and evaluation cost 170,221
micro-USD, for 402,711 micro-USD in this plan and 3,125,826 micro-USD cumulative accounted Claude
spend. This remains below the frozen 4,000,000-micro-USD ceiling.

## Outcome

The unconditional route is behaviorally validated for **lean Engram on Claude Code** in this
single repetition. Lean Engram passed both full contracts, including exact first action,
repository/component identity, evidence citation, prerequisite revalidation, safe mismatch
abstention, and matched first-attempt cobalt execution. It Pareto-dominates Claude native memory
across the two matched cases on every preregistered outcome metric.

| Layer | Full passes | Correct outcomes | Result |
| --- | ---: | ---: | --- |
| Claude native memory | 0/2 | 1/2 | abstained in both; did not inspect the tracked prerequisite source |
| Claude lean Engram | 2/2 | 2/2 | safe mismatch abstention and successful matched execution |
| Claude Engram plus native | 1/2 | 1/2 | passed mismatch; skipped Engram and abstained incorrectly when matched |

The valid comparison signal is `partial`, not portable. The combined matched lane treated the
context probe as non-actionable and emitted structured abstention without calling either
`orient` or `procedure_match`. The combined mismatch lane followed the route successfully.
Because lean and combined load the same repaired Engram contract and this plan has one repetition,
the divergence is a mixed-memory reliability warning, not evidence that native memory necessarily
degrades Engram.

In the successful lean matched lane, Claude called `orient` with the full exact request, then
called repository-local `procedure_match` with that same query despite unresolved project
linkage. Engram resolved `github.com/acme/atlas` and `queue-worker`, verified
`toolchain.toml` had `tools.version = 3`, returned the attested procedure, and directed
execution from the moved checkout root. Claude ran
`./bin/context-probe --channel cobalt` once and observed `ATLAS_CONTEXT_PROBE_OK`.
The lean mismatch lane used the same route and abstained with verified source evidence.

No completed or invalid lane may be repaired or replayed. Before a broader product claim, run fresh
repetitions of the current candidate—especially the combined arm—and re-establish the same behavior
on Codex. Do not use the older frozen repeated matrix because it predates this candidate's hashes.
The full flagship goal remains incomplete, and no live installation is implied by this result.
