# Native strict successor Stage C2B2a — Darwin host-metadata addendum review

Date: 2026-09-07 (Asia/Jerusalem)

## Disposition

The exact Darwin host-metadata design addendum below is accepted as implementation-safe within
its stated design-only boundary:

```text
evals/native_memory_pilot_v1/
NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_DARWIN_METADATA_ADDENDUM_FROZEN_2026-09-07.md
SHA-256 34ded5a82bba22ea741b8e896b15779a90893d858ed2826522b7a2b5df7d27d6
lines   893
bytes   56876
```

It narrowly amends the accepted Stage C2B2a entropy/build-ID repair freeze:

```text
evals/native_memory_pilot_v1/
NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_ENTROPY_BUILD_ID_REPAIR_FROZEN_2026-09-07.md
SHA-256 4a4dec13b99c88694625b52538b246323f69465741300c7cb9b8394cbda83e02
lines   2653
bytes   179776
```

This review authorizes only the bounded implementation work and implementation-preflight review
required by those exact bytes. It does not accept or authorize the current controller or
provisioner, a support invocation, Cargo command, build, archive, source universe, completion
audit, Linux runner, VM, provider, adapter, daemon, datastore or payload execution. It does not
accept C2B2a, the native-memory pilot or the Engram flagship goal.

The authority here is an agent-observed exact-artifact review, not human or manual-review
provenance. It cannot manufacture human approval, transfer a verdict to changed bytes or waive a
later implementation, build/source, runner, runtime or completion-audit review gate. The exact
bytes of this review record must themselves remain stable and be independently hash-checked before
they may become a scanner trust anchor.

## Independent exact-SHA review

Three independent reviewers reread the complete 893-line candidate from byte zero, verified the
same candidate and base identities, made no edits and returned no blocker:

| Independent review | P0 | P1 | Verdict |
|---|---:|---:|---|
| whole-artifact lifecycle, carrier and evidence review | 0 | 0 | accept/freeze |
| Darwin xattr, ACL, Cargo and scanner preflight review | 0 | 0 | accept/freeze |
| formal trust-closure and post-exit containment review | 0 | 0 | accept/freeze |

The reviewers independently confirmed that the accepted bytes:

- replace the impossible blanket zero-xattr predicate with exact path-class profiles and canonical
  bounded sidecars, without authorizing xattr removal or synthesis;
- bind the observed `/private/tmp` ownership/ACL transition and APFS projection while retaining
  no-follow, same-device, identity, mode, ownership and ACL-free accepted-root gates;
- constrain Cargo's four exact backup-marked final directories to pinned source and callsites,
  with three bounded in-flight names and exact marker bytes; retain the one authorized acquisition
  registry fetch, while qualification and host validation admit no filesystem- or network-
  effecting registry or Git path;
- preserve a finite nonrecursive carrier order across ordinary phases, `graph-finalize`,
  `source-audit`, `pre-runtime-finalize` and the external completion audit;
- reproduce the accepted controller entry contract exactly, including `/usr/bin/env -i`, the six
  environment assignments, Python `-I -B -S`, cwd, umask and descriptor state;
- close scanner capture and publication with concurrent bounded pipe draining, EOF and process-
  group reaping, normal exit zero, empty stderr, complete canonical parsing and descriptor-relative
  create-new mode-`0600` writes followed by sync, reopen and rehash;
- limit scanner reads to inherited source/tool/root trust checks, exact final evidence roots and
  the accepted addendum and review trust anchors, with every other repository path forbidden; and
- compare persisted properties byte-for-byte while using an explicit schedule, write-closure,
  reviewed-source and same-UID containment proof for raw stat or ACL values that died with the
  earlier controller process. It makes no invented temporal-equality or syscall-history claim.

The payload sidecar identities were independently reproduced as:

| State | Objects | Rows | Bytes | SHA-256 |
|---|---:|---:|---:|---|
| current payload tree | 29 | 59 | 6937 | `6a85bb9c4c07f42e8def7868d312769a1cdbbecda4e2d304e51ec47013321742` |
| virtual exact `build-support/archive-manifest-v1.tsv` addition | 30 | 61 | 7193 | `686b3afc9567237fca0f0c329d5599f69d12773553fce3961afcc0cb1482c18f` |

The second row uses the actual relative path
`build-support/archive-manifest-v1.tsv`; the earlier root-level serialization and its 7,179-byte
identity are rejected.

## Review ratchet

No verdict transferred across any edit. Every rejected snapshot below remains rejected and
authorizes nothing:

| Candidate SHA-256 | Lines | Bytes | Fresh review outcomes | Disposition |
|---|---:|---:|---|---|
| `3a20c3039c1291d458cf9a920d58ed6dee56ab81381d936af1fdd0e81a37df49` | 431 | 25805 | P0=0; P1=5 and P1=8 | reject |
| `ec5dc658ebad88983f01550a450f479ce4f216190985699ff6e2406b6cf8cbe3` | 726 | 44583 | P0=0; P1=7, P1=3 and P1=1 | reject |
| `51eb11efb68a3de2705195877ce70a8677c78f48547e231ca916ccd8a56e675b` | 825 | 51029 | three reviews each P0=0/P1=2 | reject |
| `2fd09a3f5f6f5c2eaf6d0fe3804d3288fb25e0085cfa86182d9c3548ae9ac5ff` | 869 | 54811 | P0=0/P1=1, P0=0/P1=1 and P0=0/P1=2 | reject |

The ratchet successively repaired incomplete Darwin metadata and evidence closure; lifecycle and
carrier contradictions; the exact controller launch and stdout-authority boundary; descriptor-
relative bounded capture/publication; a literal trust-anchor read-set conflict; and an impossible
comparison with stat/ACL values retained only in an exited process's memory. All three clean
reviews apply only to the final `34ded5a8…` bytes.

The first review-record snapshot, SHA-256
`acc7ae208fa6831c68dbf7235588ef4433ed71dc9c03700b8efc49dae74a7698` (143 lines,
8,515 bytes), also remains rejected. Its three fresh reviews returned P0=0/P1=2, P0=0/P1=1 and
P0=0/P1=0. It overgeneralized the qualification/host-validation no-registry-effect rule across the
authorized acquisition fetch and falsely omitted live Engram orientation and its ordinary
service-side effects.

The second review-record snapshot, SHA-256
`c5d8911afd783a6ecb1a8054c67952e1f4a74a01a28ad710e6920b5a643938de` (155 lines,
9,374 bytes), also remains rejected. Three fresh reviews each returned P0=0/P1=1: it corrected the
Cargo scope and disclosed orientation, but falsely called that operation wholly read-only and
claimed that no Engram write occurred even though successful orientation durably saves normal
harness telemetry.

The third review-record snapshot, SHA-256
`2ccafb2b30b1001b1c6f9a3466538e2f5ff11d01e2910cb8e6f2c039d3ed1bcd` (169 lines,
10,242 bytes), also remains rejected. Its three fresh reviews returned P0=0/P1=0, P0=0/P1=0 and
P0=0/P1=1. It disclosed telemetry persistence but falsely conditioned the registered-checkout save
on Git-state drift; the reviewed implementation updates and saves readable Git observations and
their timestamps without comparing them with prior values.

The fourth review-record snapshot, SHA-256
`a2cd4612d6d775f6b9062ccc7931e89171a24420caf074d3673abe158f943723` (178 lines,
10,861 bytes), also remains rejected. Its three fresh reviews returned P0=0/P1=0, P0=0/P1=1 and
P0=0/P1=0. It accurately described Engram's writes but overclaimed repository-read-only behavior:
the plain Git status used during orientation can refresh cached index metadata and write
`.git/index`.
No review-record verdict transfers to the corrected bytes.

## Local checkpoint evidence

At acceptance, the protected repository inputs retained their accepted identities:

| Input | SHA-256 |
|---|---|
| workspace `Cargo.toml` | `ec61531f8ffd401211f649c7b91e28a4ee5f0fa1a0fe2f199c63a841c9724f5a` |
| workspace `Cargo.lock` | `0eb924bb9008bb417cb9990081df8429520131aa6b3f7367866c5b805ec474de` |
| `engram-eval/Cargo.toml` | `4176c6a4886492f7d5adaa0bdc3134d7e04bfc8d1536de4d5eb8d4b8985ce2bb` |
| `engram-eval/src/native_vm.rs` | `7236583a702a85998e1be5a4c32950df239d32a384d4bf82e2b9dc961c90b69a` |
| `engram-eval/src/native_c2b2a.rs` | `3501336994fa7ede67c51c1fd0b7b6dc0e94595dce058d434d63edadd0122904` |
| `engram-eval/src/bin/native-c2b2a.rs` | `f2c3704cae113330674cea0d1724ee7befd6c5c8c548e7963289b55c6f90ac27` |
| `engram-eval/tests/native_c2b2a_foundation.rs` | `4f7bd87691c18d5ce2d82634415ad6635cd08d0e6fa7ad92388daaf21cfa1638` |

The current hand-authored support checkpoint is deliberately non-runnable and remains only an
implementation starting point:

| Support file | Lines | Bytes | SHA-256 |
|---|---:|---:|---|
| `build-support/controller.py` | 9308 | 370229 | `61891f26c80022230a1bb8e64fc7e9431bed688053f45aecd696e007646f2e2c` |
| `build-support/provision.py` | 2851 | 111153 | `5268ff11a2ce727cf95fd05bf6aaf02d8f2ebe549da8805d912f2459ed9084a2` |
| `build-support/entropy-failure-probe.cc` | 28 | 612 | `a85a76c0b08daec2b278ae315b7d90ad256d1c81948defdca36fbcd63293c713` |
| `build-support/entropy-failure-probe.rs` | 523 | 16376 | `4ee2d8753d302314436c902105a2b79d12425cf0f011d52544f0313954104b05` |
| `build-support/root-cargo-config.toml` | 8 | 137 | `979a4bdab6acb7eccf69cf0604758932d6f4f6e59fb4d878d66eb75044bb52cb` |

`build-support/archive-manifest-v1.tsv` remains absent and must be generated only by the later
accepted workflow. The completion-audit TSV, digest and completion-review paths remain absent.
Repository `target/debug` remains absent. The selected filesystem reported approximately 220 GiB
available. Live Engram orientation occurred during the iterative agent reviews. Each successful
orientation durably saves normal `brain_harness_trace` telemetry. For these cwd-scoped reviews,
repository resolution also refreshed and saved Engram's internal checkout record, including its
observation timestamps; that write is not conditional on branch, HEAD or dirty-state drift.
Repository resolution ran read-oriented Git commands including plain `git status --porcelain`
without disabling optional locks, so Git could refresh cached metadata and write `.git/index`.
No candidate or worktree file-content or pilot run-artifact mutation was observed or authorized by
those operations, but this review makes no repository-metadata immutability claim. None of those
contextual effects can satisfy an acceptance gate. Returned trace IDs included
`01a07bf9-11d0-7a13-936b-512f4a0209de`,
`01a07c07-0b32-7d61-bfa4-f1ebd92972f0` and
`01a07c20-3e5a-7ee1-a2de-88a0240b8943`. This list is deliberately non-exhaustive because a later
exact-record reviewer may perform the same non-authoritative lookup. No support script, Cargo
command, build, provider, VM, adapter or payload execution or pilot datastore mutation ran during
this review.

## Next boundary

Implementation may now change only the exact hand-authored support files and tests permitted by the
accepted base and addendum. Before any support invocation or Cargo command, implementation
preflight must bind the complete source hashes, launcher/capture implementation, serializers,
Darwin APIs and ABI, exact applicability table, closed schemas, static scans and focused tests and
must itself receive a fresh exact-SHA P0=0/P1=0 review.

The implementation must preserve user-owned worktree changes, protected identities, absent audit
outputs and absent repository `target/debug`. Provider-free build/source identity, Linux runner and
runtime stages remain separately gated. No later stage may claim C2B2a, the pilot or flagship goal
completion without its own exact evidence and review.
