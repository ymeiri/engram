# Schema-11 Claude unconditional-route diagnostic invalidated before lane 2 — 2026-08-18

This forward-only diagnostic is frozen at:

`/private/tmp/engram-native-memory-pilot-v1-claude-unconditional-route.pn8G8r/run-plan.json`

Prepared run-plan SHA-256:

`207d3c9d45bbd7947a2d1d4b7294caf7cdaac14a7727c5f8dce7fe2eda9c54e3`

The plan contains the same complete source-backed matched/mismatch pair across Claude Code native
memory, lean Engram, and combined memory as the preceding diagnostic: six fixed lanes and one
repetition per case/arm pair. It changes only the fresh candidate host boundary. Every actionable
repository task now requires one bounded repository-local `procedure_match` after orientation,
using the full exact user request, even without learned/remembered wording. `memory(action=list)`
is explicitly not a substitute.

This remains a single-host diagnostic. It cannot establish portable incremental value or a
statistically powered product claim, regardless of outcome. It must not replay or reclassify either
completed predecessor matrix.

## Frozen identities and provider-free gates

- Protocol:
  `evals/native_memory_pilot_v1/protocol-schema-11-claude-unconditional-route-diagnostic.json`,
  SHA-256 `19aaa2ad8ac482d89322253ef1577b39535930e9e33cd6d324f91f4fc3174f9e`.
- Engram: `1bde61b2d999ec55a68e6b3cd9a09be8e4aa23c56bd5bddebe07b8ea9322a322`.
- Evaluator: `8034dc2b2af4207df7c05d85f6630178a7a919d8014d651c868e2ecde47ce23e`.
- Claude Code 2.1.234:
  `08d8700313697cbe730a25420c908a299ce52d56f0eb2cf4fac94cab5109bc57`.
- Codex 0.148.0-alpha.9, attested but unused:
  `6170ff5578170ee9b74ad92bfcff96e6186f41d02b60815a7c2b01ad424c754f`.
- Generated Claude adapter:
  `82b51fb0a093021b309b0c2faf5604597615e09e250e983a92c5dfd23d5798c1`.
- Effective six-tool schema:
  `b704fac7da773e703dfca6bd3e9b0ffa6dcade6b259e7290315d969de3cec5b3`.
- Effective agent instructions:
  `09be365fafaf2542d137eb69384b1db1b1ac2166c1d6a71a6995cf84a27c3eac`.

Runtime attestation is `verified=true`, including exact effective-runtime hashes and successful
restricted-tool and review-authority rejection probes. Provider-free authentication readiness is
`ready=true` with no Codex lanes. The untouched audit is `invalid=false`, with six prepared
lanes, zero failures, zero native-memory write attempts, and every teaching/evaluation trace still
pending. No lane has an activation command.

The exact benchmark-wording adapter and ambiguous-project orientation regressions pass. The full
`engram-index` suite passes 271 tests with one model-download test ignored, its doc test passes,
and all 57 `engram-cli` tests pass. Affected-package Clippy passes with warnings denied;
formatting and `git diff --check` pass. Repository `target/debug` remains absent.

## Budget and authority boundary

Prior accounted Claude spend is 2,689,883 micro-USD. The plan allocates at most $1.00 additional
Claude spend and retains the $4.00 cumulative ceiling, leaving 1,310,117 micro-USD of ceiling
headroom. Its 12 teaching/evaluation calls each have a $0.083 turn-boundary allocation. The user's
standing AI-execution authorization satisfies the provider gate; all exact budget, hash, lifecycle,
trace, and outcome gates remain mandatory.

No live adapter, settings, hook, skill, daemon, or harness installation is authorized or performed.

## Invalid terminal result

Teaching stopped after the first provider call. The lane-1 Claude process exited 0 and reported
33,232 micro-USD, but its init event records the Engram MCP server as `failed`; only Bash, Read,
Edit, and Write were available. Claude therefore ran the taught amber and cobalt commands but
correctly reported that it could not perform the required Engram capture. The required datastore
and trusted procedure verification were never created.

The evaluator then ran the attested cleanup command. It exited 1 because no daemon port file
existed—the daemon had never started. The audit is consequently `invalid=true`: lane 1 has a
valid teaching trace but a missing required Engram artifact; lanes 2–6 remain untouched and
prepared. There is no phase report, evaluation, recovery, replay, or native-memory write attempt.
This plan is terminal and must never be resumed or repaired.

Immutable failure evidence:

- invalid audit: `e668c8b6da81f7a5acacc90f58b8f3864d66b9ca5eea80c779e1804e53f436fb`;
- lane-1 teaching trace:
  `1430c20b11256c291e59731566c0c136ac9e2a0ae11f7099ea38f838f05b1e81`;
- empty provider stderr:
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`;
- cleanup stderr:
  `e293078b04f3fcb33c08a2155d7ded1b21d2c1033096c5ab3e57572b472e7119`.

An isolated, provider-free reproduction found the exact startup failure: only 15,202,795,520 bytes
were available while Engram's persistent-store reserve policy required 19,893,251,686 bytes. After
the already-sealed predecessor's disposable 14.0-GiB build target was cleaned, free space rose to
27 GiB and the same candidate daemon started and stopped successfully in a fresh isolated home.
The traces and reports of both completed and invalid diagnostics remain intact.

The evaluator now checks Engram's own filesystem-headroom policy across every Engram lane before
the first provider process. Insufficient space produces a fail-closed pre-provider error naming the
lane, available bytes, and required bytes. The focused regression and all 115 evaluator tests pass;
evaluator Clippy passes with warnings denied, as do formatting and diff checks. This infrastructure
fix does not alter or validate the unconditional routing behavior.

Accounted cumulative Claude spend is now 2,723,115 micro-USD. Any successor must freeze that exact
prior spend, retain the $4.00 ceiling, use fresh lanes, and re-attest rebuilt binaries. It may reuse
the unchanged matched/mismatch protocol semantics but must never reuse this plan or its lane paths.
