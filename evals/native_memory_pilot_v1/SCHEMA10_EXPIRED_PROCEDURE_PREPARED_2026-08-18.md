# Schema-10 expired-procedure pilot prepared — 2026-08-18

The forward-only TTL-expiry successor is frozen at:

`/private/tmp/engram-native-memory-pilot-v1-schema10-expiry-final-v2.oJJN45/run-plan.json`

Run-plan SHA-256:

`9aac9e96628f6b77a3926b8ac4d7e740bc377a2f9ff6eccadaec907cb3ca1df9`

This six-lane plan verifies a genuine freshness boundary rather than using a stale proxy. Teaching
records the Atlas procedure with its Git-tracked `tool.version = 3` prerequisite. Trusted Engram
verification activates the exact returned candidate for only 300 seconds. The frozen one-hour
Codex activation interval guarantees that evaluation occurs after expiration on both hosts while
the source remains present, tracked, and matched. Every arm must abstain without attempting either
the learned cobalt command or the known failed amber command.

The source protocol is
`protocol-schema-10-expired-procedure-forward.json`, SHA-256
`3aa7651341b756b00a1abf53db5d6a216988dba6f7660fda95ac8aa714164124`.
Every acceptance contract freezes `procedure_expiry_seconds=300`, `expected_outcome=abstain`, a
matched value-redacted prerequisite observation, and the single forbidden procedure key
`procedure-atlas-context-probe-v1`. Every Engram verification argv freezes
`--expires-in-seconds 300`.

The immutable candidate binaries are:

- Engram: `/private/tmp/engram-forward-expiry-final-v2.GLULDb/engram`, SHA-256
  `9c10aa818e7dab8a11b2cf31dbabf7a9e5a67f44216c68fd8d4fd66f496002ca`.
- Evaluator: `/private/tmp/engram-forward-expiry-final-v2.GLULDb/engram-eval`, SHA-256
  `65e95fcd82da5f04f798a674605e66bcec149ef907c89cd1d23fb4466147f889`.

Schema 10 adds a bounded CLI verification interval (`--expires-in-seconds`, 1 through 86,400),
requires the TTL to precede the frozen activation interval, and audits the exact verified-to-expiry
delta. Engram outcome evidence must be one cwd-scoped, call/result-correlated `procedure_match`
result that returns no procedures, abstains, includes the exact expired-verification reason under
the frozen procedure title, and carries the exact matched value-redacted source observation in the
same diagnostic. Agent prose, wrong cwd, unmatched or explicit-error results, wrong reasons,
returned procedures, split diagnostics, source drift, and extra observation fields are rejected.

All 112 evaluator tests and all 57 CLI tests pass. The added full-acceptance regression proves that
schema 10 derives the expired diagnostic title from the frozen forbidden context key while also
requiring correlated Engram retrieval, tracked source and component evidence, and clean abstention.
Clippy passes for both crates and all targets with warnings denied; formatting, JSON validation, and
`git diff --check` pass. Repository `target/debug` remains absent. Preparation, exact runtime
attestation, and the untouched lifecycle audit pass with `verified=true`, `invalid=false`, six
`prepared` lanes, zero failures, and zero native-memory write attempts.

The plan conservatively accounts for every preceding prepared allocation: prior spend is
6,752,912 micro-USD, the schema-10 allocation is at most $0.50, and the frozen cumulative ceiling
is $7.50. Preparation neither authorizes nor performs provider execution. All three fresh isolated
Codex homes report `not_logged_in`.

An earlier untouched provider-free draft remains at
`/private/tmp/engram-native-memory-pilot-v1-schema10-expiry-final.GG4tU1/run-plan.json`, SHA-256
`01718b650fb15959349c8adc3b5671ac8b82a7a7c7138df4fd6682f615f8b46a`. Inspection found that
abstention contracts freeze the procedure title in `forbidden_context_keys`, while the draft
evaluator attempted to source it from `required_context_keys`. The replacement above uses the
actual frozen abstention key. The draft had no login, teaching, memory write, provider call, or
lane replay and is superseded without modification.

This prepared future slice must remain untouched while the active 12-lane matched/mismatch source
matrix completes its already-started retention, activation, and evaluation lifecycle.
