# Native strict successor Stage C2B2a — enforcement closure successor

Date: 2026-09-10
Status: closure-successor frozen candidate design; no implementation or execution authority

## Disposition and rejected predecessors

This amendment replaces recursive launcher self-authentication as permission with an externally
rooted exact-hash two-manifest lifecycle. Candidate self-checks remain fail-closed regression
evidence: failure blocks; success never grants execution.

The immediately preceding enforcement-successor epoch is rejected and preserved byte-for-byte:

```text
SHA-256 5fac7622f192d42baf29d1ccaacb5abd3179d87ac8471286eaf6d206e9ce43b3
lines   591
bytes   35226
path    evals/native_memory_pilot_v1/NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_EXTERNAL_SOURCE_AUTHORIZATION_BOUNDARY_TWO_MANIFEST_ENFORCEMENT_SUCCESSOR_FROZEN_2026-09-10.md
state   rejected; no review, implementation, test, or execution authority transfers
```

Its blind reviews were:

```text
Codex normative review   FAIL  P0=0 P1=2 P2=1
Codex guard review       FAIL  P0=0 P1=3 P2=2
isolated Claude review   FAIL  P0=0 P1=1 P2=5
aggregate gate           FAIL
```

No verdict transfers. Exact 5fac contained a self-referential interpreter-policy identity model,
ambiguous union/reuse rules for transitive authority, a design-review/Gate0 bootstrap gap, a
nonexistent standalone P implication, an ambiguous former completion-output symbol, restart
reconstruction risk, and insufficiently external watchdog/admission timing. These are boundary
failures; 5fac remains rejected raw evidence.

Earlier rejected raw files remain immutable:

```text
SHA-256 bed960697d497a8b721e53a7d9bad7a084c9a1e169339c290a05cf2ee9446bd6
lines   677
bytes   41736
path    evals/native_memory_pilot_v1/NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_EXTERNAL_SOURCE_AUTHORIZATION_BOUNDARY_TWO_MANIFEST_ENFORCEMENT_FROZEN_2026-09-10.md

SHA-256 74c883fec8e075ddc20a5977fc5a36751f156e1a170554b3a414f3aca27fd0e2
lines   733
bytes   48654
path    evals/native_memory_pilot_v1/NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_EXTERNAL_SOURCE_AUTHORIZATION_BOUNDARY_TWO_MANIFEST_FROZEN_2026-09-10.md

SHA-256 f1d7ddc8371c2a035f527f6d1784fe3f225cc89b3e0bac23e1616bc1f7a8ebaf
lines   690
bytes   47347
path    evals/native_memory_pilot_v1/NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_EXTERNAL_SOURCE_AUTHORIZATION_BOUNDARY_SUCCESSOR_FROZEN_2026-09-10.md

SHA-256 7d0ae76f3fd721d61fc3797d3627c4cc4a99d413e7f2c39e4ae47417dc42ec80
lines   556
bytes   36410
path    evals/native_memory_pilot_v1/NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_EXTERNAL_SOURCE_AUTHORIZATION_BOUNDARY_FROZEN_2026-09-10.md

state   all rejected; no authority transfers
```

An older rejected epoch was recorded as SHA-256
`1cd2c842fa4fa25415b03623d71458f0377c7cbd73d0de40b1bfb0a79cd5f689`, 566 lines, 38,215
bytes. Its unavailable bytes confer no partial authority under any interpretation. No property here
depends on a defect characterization; its digest/review trace are provenance only.

This closure successor is itself only a candidate. It authorizes no import, compilation, AST parse,
Q fill, candidate/static/build/runtime/provider/pilot execution, publication, VM, daemon, adapter,
datastore, or setting change until its exact bytes and separate design-review bootstrap pass.

## Exact inherited authorities and current candidates

This amendment remains subordinate to the exact inherited authorities, except only for named
supersessions below:

```text
8bed5246e2969511002ebff76fdd898abbf4316f3036393714c8a89f822f83ab  513/34249  NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_COMPLETION_LAUNCHER_BOUNDARY_FROZEN_2026-09-07.md
352b47668388c8c8a7105e579597fcc41f0e11e202f7cb03f68b8e798b6af82e  133/8512   NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_COMPLETION_LAUNCHER_BOUNDARY_REVIEW_2026-09-07.md
7156d2fcfc347aef901a1afc4888c6027123a1a41f03046bc7343859435b456f  1566/103684 NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_RETAINED_STATE_SOURCE_IDENTITY_RECONCILIATION_FROZEN_2026-09-08.md
9473072aaa88fc536df81495491521bd08cc04e642bffbbf33677f70bc9019b2  217/13955  NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_RETAINED_STATE_SOURCE_IDENTITY_RECONCILIATION_REVIEW_2026-09-08.md
9cd1ec0211f53e3239bd36a3eb057bbf539e956727ef5f474dc41acc5ec8473f  1297/88630 NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_RETAINED_STATE_CARRIER_FROZEN_2026-09-08.md
26229d625abfe557d4a41c97d3809ce2f8333d8f249bba8151aee319c73168fd  182/13449  NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_RETAINED_STATE_CARRIER_REVIEW_2026-09-08.md
```

All paths above are under `evals/native_memory_pilot_v1/`. Current candidates remain:

```text
b5e50318b84a1ba442500781bef4bf405559a43fca46737d8f82b1d236fe2530  34415/2096688  evals/native_memory_pilot_v1/native_c2b2a_completion_audit_launcher.py
eefa6eecb239ee1df49ba51317702cf78199edb7432ce32314b46a8db726b9f9  27130/1067693  engram-eval/native-c2b2a-payload/build-support/controller.py
aabe6b17ac366036a7cef05826106ea1cb6b8a30e5437937b13320ca44599261  4349/160871   engram-eval/native-c2b2a-payload/build-support/provision.py
state   all unexecuted candidates; controller still has the zero PREFLIGHT_AUTHORITY_SHA256 expression
```

This successor path is exactly:

```text
evals/native_memory_pilot_v1/NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_EXTERNAL_SOURCE_AUTHORIZATION_BOUNDARY_TWO_MANIFEST_ENFORCEMENT_SUCCESSOR_CLOSURE_FROZEN_2026-09-10.md
```

Its prospective consolidated design-review path is exactly:

```text
evals/native_memory_pilot_v1/NATIVE_STRICT_SUCCESSOR_STAGE_C2B2A_EXTERNAL_SOURCE_AUTHORIZATION_BOUNDARY_TWO_MANIFEST_ENFORCEMENT_SUCCESSOR_CLOSURE_REVIEW_2026-09-10.md
```

It remains absent.

## Trust boundary and self-proof limitation

B5 helper/matcher checks had narrow passes, while broad review showed reachable `__code__`
replacement and builtin shadowing could preserve checked source but alter behavior. A co-editable
validator cannot bootstrap permission. All recursive source/matcher/vector/receipt success is
rejection-only.

The root is an honest coordinator retaining accepted exact hashes out of band. Assumptions include
collision-resistant SHA-256; honest reviewed interpreted guard/bootstrap/phase guard; pinned
interpreter; honest kernel/filesystem/durable storage; and exact-byte reviewers. Nonclaims include
compromised roots/reviewers/host, same-UID injection or pathname swap-and-restore, malicious
same-UID precreation, lying storage, and ambient same-UID access without an OS sandbox. Owner-private
paths are not same-UID authentication. No detached-signature or whole-host-closure claim is made.

## Interpreter policy v2 without self-identity

There is no native guard or enforcement compilation branch. The sole enforcement runtime is exact
interpreted source under a pinned interpreter and canonical
`c2b2a-interpreter-tcb-policy-v2`. Final policy bytes are frozen before Gate0 reviews begin and are
an explicit exact Gate0, Gate1, M_static, M_completion, E1, and E2 input.

Policy v2 bytes contain only:

1. schema;
2. interpreter semantic payload: required implementation/version/ABI/features and isolation flags;
3. loader/shared-cache semantic classification;
4. import-resolution semantic rules;
5. closed site-package and plugin policy; and
6. exact ordered closed unpinned-standard-library TCB module/path semantic list.

They never contain the policy file's own path, device, inode, UID/GID, mode, nlink, size, LF/byte
count, content hash, or external identity row. Policy v2 contains no internal payload digest; this
avoids a digest-field self-cycle. Its complete file identity/hash/counts are bound only by external
Gate0, phase-manifest, and envelope DAG rows. The stdlib list is literal: no prefix/glob/optional/
caller expansion. Policy/interpreter drift invalidates Gate0 and every later gate.

Each manifest inventories every candidate-controlled semantic input: repository, generated/
writable, plugin/hook, import path, config, data, executable/script, environment, argv, cwd, FD,
presence/absence, metadata, ordering, and directory resolution. Only kernel/syscall/filesystem,
dyld/shared cache, and exact policy-listed stdlib identities are ambient TCB. Ambient TCB cannot
launder another input. Inclusion of provisioner/controller/runner/build/anchor/directory is exact
iff the reviewed invocation references/opens it. This is finite semantic closure, not whole-host.

## Bootstrap-safe consolidated design review

For this successor, the three future exact-amendment review results, when produced, are external
reviewer observations, not required pre-existing files. Their eventual exact observation identities
remain outside `_AUDIT_PARENT`; none of the rejected predecessor reviews can occupy these roles.

The sole consolidated review leaf uses exact `c2b2a-design-review-v1`. Its canonical fixed-order
content is: schema; exact amendment path/type/device/inode/UID/GID/mode/nlink/size/hash/LF/byte
identity; then exactly three ordered reviewer-observation identities with harness/provider/model/
session/times, P0/P1/P2, verdict, and findings digest. It contains no self-hash, future Gate0/Q/L/E
hash, or peer-derived expected identity.

This amendment freezes the review leaf's exact audit-parent identity, basename, requested regular-
file mode `0644` compatible with the repository document policy, maximum LF/byte bounds, required
absence, create-new/no-overwrite rule, and partial-or-invalid-is-terminal rule. It does not claim
that the record inherits a later Gate0 protocol. A
candidate completed record may be created once, then independently full-read/hash/count/stat/
pathname-versus-FD verified. It confers no authority merely by existing.

After the completed review record has a stable exact identity, three fresh independent clean
reviews of that exact SHA must accept its content and bootstrap semantics, across at least two
harness/provider families. Those later observations remain external and no recursive second review
record is created. Only the coordinator's out-of-band accepted exact review hash is the trust root.
Later Gate0 and envelopes bind that accepted file. Failure preserves it as rejected evidence and
authorizes nothing.

Gate0 and later canonical attestation artifact paths/schemas/atomic protocols are frozen by Gate0
machinery. Individual design observations and all later attestation/enforcement artifacts are
external; only this amendment leaf and consolidated design-review leaf are inside audit-parent P.
There is no generic "design namespace" claim.

## One canonical rooted DAG-union grammar

All E1, I_static, and E2 transitive authority use one grammar,
`c2b2a-authority-dag-union-v1`. There is no alternate prose closure or list concatenation.

An artifact-row key is exactly `(absolute_raw_path_bytes, st_dev, st_ino)`. Content-equal files at
different keys are allowed. A second path to the same device/inode, hardlink, alternate spelling,
case/normalization alias, symlink resolution, or ancestry alias is terminal. Repeated raw path with
different device/inode is likewise terminal path rebinding. Multiple labeled parent edges to the
same key reuse one row index. Repeated discovery of a key must produce a byte-identical complete row
and byte-identical ordered outgoing-edge set or terminate.

Each row contains fixed-order rank, role, length-prefixed raw absolute path hex, type, device/inode,
UID/GID/mode/nlink/size, LF/byte counts, content SHA-256, and ordered outgoing edges. Every edge has
a length-prefixed label and child row index. A row's final preimage digest is SHA-256 over the exact
length-delimited preceding row fields, excluding that digest field. No row hashes itself.

Leaves have rank 0. Each parent has rank `1 + max(child ranks)`. Canonical rows sort by increasing
rank, then raw path bytes, then device/inode; edge labels sort by raw label bytes, then child key.
All edges point to a lower rank and earlier row index. Unknown/extra/unreachable row, missing edge,
duplicate key/label, forward/same-rank edge, inconsistent repeat, digest mismatch, or cycle is
terminal. Every row must be reachable from at least one explicit ordered root. Traversal performs
one union and serializes each keyed object once.

E1 envelope is the external authenticated root and embeds dependency rows, but no row for its own
file. Its explicit roots cover accepted consolidated design review, Gate0 acceptance, policy,
guard/bootstrap/phase guard, run plan, M_static, Gate1 attestations, amendment, and every dependency.

I_static's graph adds exactly one distinct now-existing `E1_ROOT` artifact row plus roots for S1,
static guard envelope, static coordinator receipt, raw FD1/FD2, and exactly three post-static
attestations. Traversal of E1_ROOT recursively reuses E1 dependency rows. I_static does not add a
separate M_static root or duplicate any E1-bound artifact. M_static is reached through E1_ROOT.

E2's explicit roots are exactly E1_ROOT, I_STATIC_ROOT, ordered pre-runtime receipt roots,
PRE_RUNTIME_FINALIZE_ROOT, M_COMPLETION_ROOT, and exactly three Gate2-attestation roots. Traversal
unions them once and reuses every repeated dependency. E2 does not separately add M_static or an
"all I_static-bound" list. The E2 envelope is not its own row.

There is no separate ambiguous Gate1 input digest. I_static binds `e1_dag_stream_sha256`, defined
as SHA-256 of the complete canonical DAG-union byte stream embedded in E1. The I_static digest field
is not part of that preimage. Parser must reconstruct the exact E1 stream, recompute, and require
equality.

## Lifecycle ranks and acyclic envelopes

The lifecycle rank table is normative:

| Rank | Newly accepted objects |
| ---: | --- |
| 0 | closure amendment plus three external amendment observations |
| 1 | completed consolidated design-review leaf plus three external exact-record acceptances |
| 2 | final interpreter policy v2, interpreted guard/bootstrap/phase guard, Gate0 attestations |
| 3 | atomic Gate0 acceptance |
| 4 | inherited Q/R0/D/L/H/R construction yielding final L and R |
| 5 | run plan, M_static, Gate1 attestations, E1 |
| 6 | S1, static trace/receipts, post-static attestations, I_static |
| 7 | ordered pre-runtime receipts and pre-runtime-finalize |
| 8 | M_completion, Gate2 attestations, E2 |
| 9 | S2, completion trace and publication outputs `O_pub` |
| 10 | completion attestations and Rv instance |
| 11 | later build/source, Linux-runner, qualification, remaining gates, eventual pilot |

No higher-rank byte flows backward. Policy v2 is final before any rank-2 Gate0 review starts.
Gate0 has no dependency on future Q/run plan. Run plan contains no future M/E/I/Rv instance hash.
Manifests contain no self/E/future hash. E1 binds future S1/I_static policy only; E2 binds future
S2/Rv policy only. Future objects may bind an already-existing E hash without a cycle.

Exactly two manifests/envelopes exist: M_static/E1 for one final-L static invocation and
M_completion/E2 for one `completion-audit-publish` invocation. No third record/manifest/envelope.
E2 is completion-audit authority only, never provider/activation/evaluation authority.

## Dedicated expected-hash channels

Expected E and I_static hashes travel only on Gate0-reviewed, one-shot coordinator-to-guard
channels. Gate0 freezes one constant `EXPECTED_HASH_FRAME_MAX_BYTES`; that maximum is parser
configuration and is not serialized in a frame.

Frame bytes contain fixed-order role tag, one lowercase 64-hex hash, epoch, phase, terminal LF,
then EOF. They are never argv/environment/stdin/file/manifest/envelope/candidate/provider output.
Coordinator writes one bounded frame and closes its endpoint. Guard reads exactly one frame under
the pre-frozen constant, rejects short/extra/reordered/missing-EOF bytes, records frame hash, closes
all endpoints before candidate exec, then authenticates the held named artifact. E guard receives
only applicable expected E; each pre-runtime phase guard receives only expected I_static. Channels
are never reused or inherited.

## Interpreted Gate0 and atomic acceptance

Gate0 begins only after policy v2 and accepted consolidated design review are final. Three blind
clean reviews with P0=P1=0 and at least two families cover exact interpreted guard source, pinned
interpreter/policy, bootstrap, DAG/manifests/spends/I_static/Rv parsers, dedicated channels,
coordinator/phase guard, typed audit parent, watchdog/deadlines/resources, raw capture, cleanup, and
postchecks.

Exactly one external coordinator-issued `c2b2a-gate0-acceptance-v1` binds epoch/pass/scope, exact
reviewed input DAG, the accepted consolidated design-review file's complete identity row, and three
ordered semantic/guard/adversarial Gate0 attestations. Each identity row has path/type/device/inode/
UID/GID/mode/nlink/size/hash/counts/preimage digest. It has no self/future hash. The coordinator's
three clean external exact-review-record observations remain trust-root state, not invented files.

The exact Gate0 review inputs predeclare owner-private parent/path/basename/schema/bounds/0600/
absence. Coordinator stable-opens parent no-follow, rejects aliases, uses
`openat(O_CREAT|O_EXCL|O_NOFOLLOW,0600)`, verifies initial regular/same-device/owner/mode/nlink1/
size0/stable inode, full-writes/fsyncs, closes/reopens read-only no-follow, proves fstatat path/FD
same object, bounded-read/parses/hashes/counts, repeats file/parent fstat, closes, and parent-fsyncs.
Existing/partial/invalid output poisons the epoch. Coordinator retains expected Gate0 hash out of
band. Gate0 authorizes no candidate action; E1/E2 later bind it.

## Phase manifests and unambiguous final-L schema

Both external create-new manifests use `c2b2a-phase-manifest-v4`. Each binds one sequence/phase,
one exact invocation, complete semantic inputs including policy v2, embedded typed P, exact output
transition, canonical bounds/metadata/hash/counts/final LF. Missing/duplicate/reordered/optional/
placeholder/wildcard/prefix/caller-expanded rows terminate.

The post-Q invocation schema is only `c2b2a-final-l-path-invocation-v2`. No active profile calls
final L b5. It binds final-L exact post-Q path/hash/metadata, pinned interpreter/policy, phase/argv,
six-variable environment, cwd `/var/empty`, umask0077, visible FDs0/1/2, direct guard parent,
semantic inputs, watchdog deadlines/resources/status, and typed filesystem transition.

M_static is frozen after Gate0/final L/R/run plan and before Gate1. It has one `static` invocation,
all existing semantic inputs, and first embedded canonical P. It excludes future S1/static/I_static/
pre-runtime/M_completion/E2/S2/O_pub/Rv/provider bytes. Static final L has no filesystem output;
only bounded external raw FD1/FD2 capture.

M_completion cannot be drafted/frozen until I_static and all pre-runtime receipts/finalize exist.
It freshly inventories every then-existing semantic input for one completion-audit-publish call,
including final L, `C_inherited` (the inherited DAG controller/source node), R, referenced generated/
build/source/config/data/import rows, policy v2, and an independently reconstructed byte-identical
embedded P. It excludes future S2, `O_pub`, Rv instance, qualification, and provider bytes.

## Canonical I_static and coordinator continuity

`I_static` is `c2b2a-static-implementation-acceptance-v2`, sole coordinator-issued necessary-but-
insufficient acceptance for the exact ordered inherited pre-runtime phase list. It authorizes no
final L, completion, provider, retry, unlisted/reordered phase, or source edit.

E1/run plan predeclare only I_static parent/path/basename/schema/bounds/absence/0600/creation policy.
Canonical content binds epoch/pass/scope, run-plan path/hash and literal phase list/digest,
`e1_dag_stream_sha256`, the I_static DAG-union with exactly one E1_ROOT plus new S1/static/post-
static roots, and three ordered clean post-static attestations. It contains no self/future hash.
Coordinator creates it with Gate0 atomic protocol and retains expected hash only in trusted state.

Before each pre-runtime phase, the same authorized coordinator delivers expected hash once to the
Gate0-accepted phase guard, which closes channel endpoints, holds I_static/full DAG, and validates.
After descendants drain/reap, the same guard revalidates held rows; coordinator compares its trusted
hash and seals receipt. Neither hash nor authority artifacts reach candidate. E2 binds I_static and
ordered receipts.

Run plan/receipts bind exact coordinator instance identity and OS process-birth identity. Once
I_static exists, loss, death, replacement, exec, or restart of that trusted coordinator before
pre-runtime-finalize permanently terminates the epoch. A new instance is rejected. Expected hash is
never reconstructed from I_static, candidate bytes, filesystem hints, or receipts. Continuation
requires separately named successor. Each inherited phase also requires crash-durable at-most-one
start control or the implementation is unimplementable.

## Spend protocol and narrow same-instance recovery

S1/S2 external policies bind exact owner-private parent/path/basename/schema/mode/absence, not
future hash. Claim content binds existing E/M hashes, sequence/phase/attempt1/time/final LF.

The still-authorized guard stable-opens exact parent; missing/rebound/inaccessible parent is
terminal. It rejects aliases, uses `openat(O_CREAT|O_EXCL|O_NOFOLLOW,0600)`, verifies initial
regular/same-device/UID/GID/mode0600/nlink1/size0/stable inode, full-writes/file-fsyncs, closes/
reopens read-only, proves fstatat path/FD same object/exact canonical size, bounded-read/parses/
hashes/counts, repeats file/parent fstat, closes, and parent-fsyncs. Child admission is impossible
before durability.

O_EXCL is a live concurrency fence; authorization is durably spent only after canonical readback
and parent fsync. The absent pre-durability classifier is available only within the same still-
authorized coordinator instance after a caught non-process-fatal write/fsync failure: if a freshly
verified unchanged parent has exact basename absent, no durable admission existed and no child
could start, so attempt1 may be recreated safely. Any surviving zero/partial/valid/invalid/aliased
entry blocks or poisons. After coordinator loss/restart, no absent classifier is allowed and the
epoch terminates. A durable valid claim blocks replay. Surviving entries are never repaired/deleted.

## Independent watchdog and exact cutoffs

Guard captures trusted monotonic `t0` before expected-hash channel read, authority precheck, or
spend. It arms an independent reviewed watchdog before any potentially blocking channel read,
open/create, filesystem operation, or spawn. Watchdog owns kill/cleanup authority independent of
candidate progress. Every blocking call has trusted pre-call and post-return timestamps.

Static: expected channel/precheck/S1 and successful spawn return must complete by `t0+20s`.
Late return/start is terminal; watchdog kills/reaps the child and S1 stays consumed. Child must
terminate by `t0+140s`; all cleanup/drain/reap by `t0+160s`; authority/P/unselected postcheck by
`t0+180s`. The nominal child budget remains at most 120 seconds from trusted pre-spawn timestamp.

Completion: expected channel/precheck/S2 and final-L spawn return must complete by `t0+50s`.
Let `t_final` be guard's trusted pre-call timestamp immediately before that final-L spawn. Final L
must terminate by both `t_final+1100s` and `t0+1150s`; outer cleanup/drain/reap by `t0+1175s`;
authority/state postcheck by `t0+1200s`. Guard records pre/post spawn timestamps and rejects late
admission even if the child later succeeds. Exact reviewed final L records scanner pre/post spawn
timestamps; scanner post-spawn admission must be by `t_final+100s`, scanner termination by
`t_final+1000s`, and internal publication/cleanup by final-L termination at `t_final+1100s`, never
beyond outer cutoffs.

No clock origin resets. Timeout/reserve miss makes success impossible and consumes a durable spend.
CPU/address-space/file-size/process/FD/stream ceilings freeze no later than Gate0/manifests and do
not weaken inherited bounds.

## Typed embedded P and `O_pub`/Rv transitions

`_AUDIT_PARENT` is the sole mixed-role direct parent. For each bounded observation/static/
completion/Rv interval, guard/coordinator freshly stable-opens one no-follow parent FD, compares
path/device/inode/UID/GID/mode/nlink/ancestry to frozen identity, holds it through that interval,
repeats fstat, then closes. It is not continuously held between phases or across days.

P has no standalone path or file. Canonical `c2b2a-audit-parent-prestate-v2` bytes are embedded in
both M_static and M_completion. Rv writer holds/parses both manifests, requires embedded P bytes and
digest identical, reconstructs live P from audit-parent FD, and revalidates equality.

P sorts direct entries by raw basename bytes; names encode as length+lowercase hex. `regular` rows
bind basename/type/device/inode/UID/GID/mode/nlink/size/hash/byte count/LF count or not-applicable.
`directory` rows bind basename/type/device/inode/UID/GID/mode/nlink plus canonical subtree digest
and ordered typed children, recursing only into explicitly named dirs without symlink/device
crossing. Symlink/socket/FIFO/device/unknown type, undeclared recursion, hardlink/inode/path/case/
normalization alias, or duplicate raw name is terminal. Every P leaf is immutable.

Let `O_pub={completion TSV, completion digest}` and Rv be the one completion-review leaf:

```text
prestate:             P present; O_pub union Rv absent
child transition:     P -> P union O_pub
review transition:    P union O_pub -> P union O_pub union Rv
```

Final L creates exactly TSV then digest with frozen formats/bounds/mode/fsync order. No temporary/
third/overwrite/rename/delete/P/directory delta. TSV without digest, digest without TSV, invalid/
partial O_pub, or publication failure is terminal: preserve all bytes, create no Rv, no repair/retry.
Static produces no filesystem delta. External E/M/S/I/Gate0/attestation/trace/receipt/capture
parents remain ancestry-disjoint; audit-parent sharing is the sole parent exception, never leaf alias.

## Historical P leaves and review-output separation

Protected P entries include rejected 7d/f1/74c/bed960/5fac amendment basenames. Their five
prospective review paths stay absent/uncreated/unauthorized. Only this closure amendment and its
eventual consolidated review leaf metadata are newly frozen before R0. Six inherited runtime
anchors remain unchanged; no seventh.

Individual current design-review outputs are external observations, not P files. Gate0 and later
attestation/enforcement paths and atomic protocols are frozen by Gate0. Amendment and consolidated
design-review are the only review-related P leaves. New reviewer content/hash/verdict/findings never
enter candidate argv/env/stdin/FD/source, Q/H/R, static vectors, provider payload, or runtime; only
unchanged inherited review anchors and declared consolidated-review metadata are exceptions.

## Future Rv instance and held-input atomic creation

Before Gate2, only exact Rv parent/path/basename, `c2b2a-completion-review-v1` schema/order/content
policy, max bounds, requested metadata, absence, and creation protocol freeze. No future Rv bytes,
inode, content hash, or instance identity is predicted.

After valid E2/S2 postcheck and complete O_pub, exactly three clean sealed semantic/guard/
adversarial completion attestations bind O_pub, E2/S2, traces/receipts, deadlines, P transition, and
completion facts. They do not bind future Rv.

Coordinator alone writes Rv. For a fresh bounded Rv interval it holds M_static, M_completion,
audit-parent FD, both O_pub files, all three attestations, E2, S2, trace/receipts, and every Rv-bound
input. It parses both embedded P copies, requires equality, reconstructs/revalidates live P and all
held identities, then performs Gate0-grade O_EXCL/0600/full-write/fsync/close/reopen/readback/fstat/
parent-fsync creation. After parent fsync and before acceptance it revalidates every held input and
exact `P union O_pub union Rv`. Concurrent authorized writers are forbidden; same-UID precreation is
a nonclaim. Existing/partial/aliased Rv poisons the epoch. Rv binds the three attestations and held
input identities, never itself/future bytes. Later gates bind resulting Rv hash.

## Direct guard postchecks and gates

Guard directly parents final L under `c2b2a-final-l-path-invocation-v2`. Only FDs0/1/2 reach final
L: read-only `/dev/null` and distinct nonseekable stdout/stderr pipes. Hash/authority channels and
FDs close first. Raw streams remain separate bounded external evidence.

Before dispatch and after drain/reap, guard/coordinator expand the applicable rooted DAG union once,
hold/revalidate every row and selected manifest/spend/parent state. Receipt binds full pre/post rows,
DAG digest, expected-channel receipt, deadlines/watchdog, exact invocation once, process/session/
cleanup, selected transition, unselected unchanged, and terminal validity. Any omitted/rebound/
mutated/unreadable/unvalidated object blocks acceptance.

Gates are exactly: three amendment observations; completed consolidated review plus three external
exact-record acceptances; interpreted Gate0 reviews/acceptance; Q/final L/R; M_static/Gate1/E1;
S1/static/post-static/I_static; continuously guarded pre-runtime-finalize; M_completion/Gate2/E2;
S2/completion/O_pub; three completion attestations/Rv; build/source identity review; provider-free
Linux runner review; runtime qualification/reviews; remaining inherited gates; eventual unchanged-
pilot provider execution. Every three-review gate uses at least two families, one family at most two;
writers cannot review own artifacts; correlated-model limitations remain explicit.

E2 never authorizes provider work. Rv remains necessary but insufficient for later gates.

## Preserved inherited DAG and invalidation

The inherited DAG, with the ambiguous symbol renamed only in this amendment, is:

```text
U -> A -> C_inherited -> HC -> L0/Q -> S -> W0/F0 -> components/B/V/O
  -> R0 -> D -> G_D -> L -> H -> G_H -> R
  -> authoritative W1/F1/replay
```

`Q(L)=L0`, `P(R)=R0`; H is raw only in sole `launcher_source_sha256` removed by P. Final L after Q
is not b5; no b5 hash/review transfers. Six anchors, 26 proof rows, carrier/codecs/dominance,
non-source gates, no-repair/no-replay remain. `C_inherited` is reserved exclusively for the inherited
DAG node; publication output is always `O_pub`.

Policy/Gate0/guard/bootstrap/phase guard/coordinator identity/run plan/final L/Q/R/C_inherited/
referenced provisioner/anchor/common bound/P pre-R0/amendment/review/M_static drift invalidates
Gate0/Gate1 as applicable. Static failure stops before I_static. I_static/coordinator/phase receipt
drift terminates later phases. Post-static-only pre-runtime drift invalidates from affected phase/
Gate2 without rewriting static history. M_completion/P/O_pub/Rv/Gate2/E2 drift invalidates Gate2.

Durable spend blocks retry. Same-instance absent pre-durability recovery is not replay because child
admission was impossible; no restarted coordinator may use it. Partial O_pub/Rv or any alias,
alternate invocation, duplicate/wildcard row, unexpected delta, deadline/watchdog, cleanup, or
authority failure is terminal. Preserve all exact/partial bytes; never delete/repair/normalize/
overwrite/replay. A true-start retry requires separately named successor and fresh reviews.

Only external permission, policy v2, design bootstrap, rooted DAG union, interpreted Gate0/I_static,
two manifests, spends, watchdog/deadlines, typed embedded P/O_pub/Rv, and dedicated hash channels
supersede matching inherited clauses. All other inherited constraints remain.

## Acceptance boundary and current disposition

Before any candidate action: accept this exact amendment and completed consolidated design review;
freeze policy v2; accept atomic Gate0; construct/review Q/final L/R; then follow lifecycle ranks with
pristine identities, positive disk reserve, absent `target/debug`, combined output absent before its
sole inherited create-new publication, and every no-replay/no-repair gate.

Design acceptance alone accepts no guard/Gate0/M/E/S/I_static/source/Q/static/pre-runtime/O_pub/Rv/
provider/pilot/flagship result. This amendment adds no provider/model/budget/VM/adapter/daemon/
datastore/connector/network/auth/product behavior/user data/runtime anchor/recovery/source mutation,
and authorizes no stage, commit, or live setting change.

Its prospective review is absent. There is no execution authority, Gate0, Q fill, combined output,
manifest, envelope, spend, I_static, pre-runtime run, O_pub/Rv, or controller/provisioner change.
B5 `b5e50318...`, controller `eefa6eec...`, provisioner `aabe6b17...` remain unexecuted. Rejected
7d/f1/74c/bed960/5fac bytes remain immutable evidence.
