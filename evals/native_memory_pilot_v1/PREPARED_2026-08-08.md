# Learned native-memory pilot v1 preparation — 2026-08-08

## Outcome

The provider-free preparation and re-attestation gates passed for the current Engram 0.2.3
durability candidate after the source-attested procedure matcher v3 passed its frozen 19-scenario
matrix. The generated plan contains six matched lanes: native memory, lean Engram, and Engram plus
native memory on both Codex and Claude Code.

All lanes audit as `prepared`, with no integrity failures. `execution_approved` remains `false`, and
no teaching, activation, evaluation, agent-output, or procedure-verification artifact exists. This
is preparation evidence, not a pilot outcome or an incremental-value claim.

## Reproduction

```bash
cargo run -p engram-eval -- prepare-native-memory-pilot \
  --protocol evals/native_memory_pilot_v1/protocol.json \
  --output /tmp/engram-native-memory-pilot-v1-runtime-json.IAowd7 \
  --engram-bin /Users/yuval.meiri/projects/engram/target/debug/engram

target/debug/engram-eval audit-native-memory-pilot \
  --plan /tmp/engram-native-memory-pilot-v1-runtime-json.IAowd7/run-plan.json

target/debug/engram-eval attest-native-memory-pilot \
  --plan /tmp/engram-native-memory-pilot-v1-runtime-json.IAowd7/run-plan.json
```

The attestor returned `verified: true`. It rechecked the frozen plan digest, all three executable
identities, every host and lifecycle command, and the effective isolated stdio MCP contract.
Before regenerating this plan, the attestor rejected the previous prepared plan because its bound
Engram SHA-256 was `8e369df11cb6d3ad800ce0f9cd82a0972f74dd7a7d6b2a66bfb7e7779bbbfa62`
while the rebuilt runtime-attestation candidate was
`8680bdd7a92d43c7571c8acbdc578f8414552a62b07b611ff32fec130f3df921`. No stale
provider plan was reused.

## Attested executables and contract

| Executable | Version | SHA-256 |
| --- | --- | --- |
| Engram | `engram 0.2.3` | `8680bdd7a92d43c7571c8acbdc578f8414552a62b07b611ff32fec130f3df921` |
| Codex | `codex-cli 0.147.0-alpha.1.2` | `9f6748b4ab10ffc92c28b9ccedae89e61a302bbc011df7d276ee38f55906e481` |
| Claude Code | `2.1.224 (Claude Code)` | `391df9d2ab04e4cf32199335720ac7715a582e91eaecfd4d2198a16f57ea59b3` |

The compact Engram agent profile exposes six MCP tools. Its tool-schema SHA-256 is
`cb48eb1bc9a6d21e8aafb38a4bf012a39aceb37f63a5d7684d30d56987a2836e`, and its
profile-instructions SHA-256 is
`a608ff0614525387b6cebd89b14fc73f4c9e93b5ab00c7d5b9d260e5850037a7`.
The schema-v3 real stdio handshake reproduced both hashes, rejected a restricted administrative
tool, and rejected caller-supplied `manual_review` authority.

The deterministic fixture revision is
`7ccc358097d59ae004f7d1144c38199453c6d9851b5ae7331c3c7f42f916432e`.
The rendered Codex adapter SHA-256 is
`63d1a3ff587fd5fdb43ac416834a45ec0eb820d223867cebcbbbc279e53dd0ef`; the
Claude adapter SHA-256 is
`5f4c740f4581e89b43bcb92141c796e524d30f7bec7029bea0cce450db8839fc`.

## Frozen artifacts and privacy boundary

The generated tree canonicalizes to
`/private/tmp/engram-native-memory-pilot-v1-runtime-json.IAowd7`. Its control-artifact hashes are:

| Artifact | SHA-256 |
| --- | --- |
| `run-plan.json` | `10111f3fa105551600ffc7efa72baa00f72c5214dcc88ddbf68e880d14dd296d` |
| `protocol.snapshot.json` | `10a3ba008f070ab7ba237791ff6b3bd320ce9437177011b59c27be92c74f362f` |
| `agent-output.schema.json` | `9d6c77de084d735e628c053015737debe4c345ce3be2aef6d4b7b0da8d9f7b1b` |

Preparation now creates the pilot output root and every mutable `CODEX_HOME`,
`CLAUDE_CONFIG_DIR`, Claude auto-memory root, and `ENGRAM_HOME` with mode `0700` on Unix. The
generated plan stores only the required secret name, `CODEX_ACCESS_TOKEN`, never its value. The six
Claude calls are independently capped at $0.083, below the frozen aggregate $0.50 ceiling.

Verification:

```text
cargo test -p engram-eval
62 passed; 0 failed

cargo clippy -p engram-eval --all-targets -- -D warnings
Finished successfully
```

## Next gate

Teaching calls invoke external model providers and spend quota. They require explicit operator
approval and exact confirmation of the frozen 50-cent aggregate Claude budget. Codex also requires
`CODEX_ACCESS_TOKEN` at execution time. The runner then enforces separate teaching, one-hour Codex
activation, and evaluation approvals through lifecycle state and artifact gates.
