# Engineering context pilot v1 preparation — 2026-08-08

## Outcome

The provider-free preparation gate passed for the current Engram 0.2.3 durability candidate. The
generated plan contains eight balanced runs across Codex and Claude Code, the
`instructions_only` and `lean_engram` arms, and both flagship scenarios. Preparation did not invoke
either host: `execution_approved` is `false`, and no trace or agent-output files exist.

This is preparation evidence, not a pilot result and not evidence that Engram outperforms either
host's native memory.

## Reproduction

```bash
cargo run -p engram-eval -- prepare-pilot \
  --protocol evals/engineering_context_pilot_v1/protocol.json \
  --output /tmp/engram-context-pilot-durability-20260808 \
  --engram-bin /Users/yuval.meiri/projects/engram/target/debug/engram

target/debug/engram-eval validate \
  --manifest /tmp/engram-context-pilot-durability-20260808/manifest.json
```

Validation reported:

```text
validated engineering-context-pilot-v1: 2 scenarios, 4 arms, 1 repetitions
```

## Attested executables

| Executable | Version | SHA-256 |
| --- | --- | --- |
| Engram | `engram 0.2.3` | `2c2ee2697af56aba38c7c30d262257c082f1e1fbb151de2fd942735061f1a8b1` |
| Codex | `codex-cli 0.147.0-alpha.1.2` | `9f6748b4ab10ffc92c28b9ccedae89e61a302bbc011df7d276ee38f55906e481` |
| Claude Code | `2.1.224 (Claude Code)` | `391df9d2ab04e4cf32199335720ac7715a582e91eaecfd4d2198a16f57ea59b3` |

The deterministic fixture revision is
`7ccc358097d59ae004f7d1144c38199453c6d9851b5ae7331c3c7f42f916432e`.
The rendered Codex adapter SHA-256 is
`63d1a3ff587fd5fdb43ac416834a45ec0eb820d223867cebcbbbc279e53dd0ef`; the
Claude adapter SHA-256 is
`5f4c740f4581e89b43bcb92141c796e524d30f7bec7029bea0cce450db8839fc`.

## Frozen generated artifacts

The generated tree is under `/tmp/engram-context-pilot-durability-20260808`. Its control-artifact
hashes are:

| Artifact | SHA-256 |
| --- | --- |
| `run-plan.json` | `b6a63b635414e975b663dbc44633f9703f600ddfd8bbe033e4a2489e0b38f8d5` |
| `manifest.json` | `5f9c5e1a4d3493a0696d42adf2b9bdd6006fcb82ff02f0ff60a0de31810f4a7e` |
| `scenarios.jsonl` | `527eb36b0b897ebdccf0056a91f95fce826de9dcd1797824be61ac2a99509095` |
| `agent-output.schema.json` | `9d6c77de084d735e628c053015737debe4c345ce3be2aef6d4b7b0da8d9f7b1b` |

The plan fixes four runs per host and one run for each host × arm × scenario cell. Every Engram run
has an isolated `ENGRAM_HOME`, project store, and adapter. The aggregate Claude hard cap is $0.25;
Codex execution remains separately approval-gated because its CLI has no equivalent dollar cap.

## Next gate

Executing these eight commands calls external model providers and spends quota. It requires
explicit operator approval. The separate host-native-memory pilot is still required before making
an incremental-value claim.
