# Engram flagship completion audit — updated 2026-09-05

This audit preserves the full goal and classifies current evidence as **proven**, **partial**,
**contradicted**, or **missing**. “Proven” means the cited evidence covers the stated scope; a
provider-free test is not used to claim native-host behavior, and candidate code is not used to
claim the installed runtime has adopted it.

## Schema-15 repeated required-host-action result — 2026-09-05

The immutable exact-Bash `-04` successor completed teaching, the one-hour Codex retention gate,
activation, and evaluation exactly once. Every provider process exited 0, no lane was recovered,
replayed, or repaired, and the final frozen audit is `complete=true`, `invalid=false`, with zero
lifecycle or integrity failures. The guarded provisioner had created exactly nine owner-only,
single-link, regular, non-symlink Codex cache copies without inspecting or hashing credential
contents.

All 12 Engram-bearing lanes pass the full preregistered contract across Codex and Claude Code;
all six native-only controls fail the full contract on scoped identity and the exact evidence
route. All 18 lanes still produce the correct safe abstention, report no wrong-project context as
applied, execute no procedure, repeat no failed command, and attempt no shell write to native
memory. Native-memory artifacts exist in all six native-only lanes, so this is not an
absence-of-native-memory claim.

Every Engram-bearing lane makes exactly one identity-bearing procedure query, reads exactly one
returned local evidence source, represents project authorization as requiring confirmation, and
cites the correlated evidence. All 12 matched treatment/control pairs satisfy the frozen packet,
incremental-token, and incremental-duration budgets. The strict report records
`incremental_value_signal=engram_and_combined_across_hosts` and
`portable_incremental_value_observed=true`. `all_acceptance_passed=false` remains explicit because
the six native controls do not meet the full identity/evidence contract.

The authoritative plan is
`/Users/yuval.meiri/.engram/evals/native-memory-pilot/required-host-action-bounded-query-repeated-exact-bash-20260905-04/run/run-plan.json`,
SHA-256 `d7495a2915f3b2b4bc43e665af162b4cc95741240fecb57a22b1d1bd9e1646ea`.
The activation, evaluation, completion-audit, and strict-report SHA-256 values are respectively
`263ef1bda5238111e0b01b784b38cf04575409f2934b9cb41b1b7185c9db616b`,
`ffc55970f8c225160169f51026e749c15492572754452c05bf10613d8fd58182`,
`4cc7e34b554cdf2db8d776e342f17530f2f4dfe0f5576410e1902aa466367c65`, and
`ac43cace5b008eb9281c1036a9f2b1a4f84a947bd7aae4644357766c2204ed92`.
New Claude cost was `$0.729603`; cumulative accounted spend is `$7.822093`, below the `$10.00`
ceiling. Full evidence is in
`native_memory_pilot_v1/SCHEMA15_EXACT_BASH_COMPLETED_2026-09-05.md`.

This closes the repeated required-host-action wrong-repository-scope slice with a valid positive
treatment/control result for structured identity and exact evidence-route compliance. The matched
outcome-correctness delta is zero because every arm safely abstains. Claude native responses cite
stale Atlas memory, and the combined lanes lack evidence-bearing competing native memory, so this
does not prove zero stale influence or stale-native override. The evaluator's “portable” field
means across the two agent harnesses on one physical machine; it does not prove
independent-machine portability or statistical generality. The flagship verdict remains **not
complete** because native stale/expiry, actual
Claude compaction and controlled Codex process-boundary continuity, native correction behavior,
safe procedure correction, and independent-environment replication remain open. The bounded
proposal/operator correction candidate is installed and live-attested, but has not yet passed a
controlled fresh-session trial on either host.
The preregistered secret-canary gate and Engram-owned deletion propagation are already proven
within their explicitly bounded claims.

## Schema-14 source-bound single-call successor — 2026-09-04

The forward-only comparison completed teaching, the exact one-hour retention gate, all three Codex
activations, and all six evaluations exactly once. Every provider process exited 0, no completed
lane was recovered or replayed, and the frozen audit is `complete=true`, `invalid=false`, with zero
lifecycle failures. Final frozen re-attestation and all three isolated Codex ChatGPT checks pass.
All six agents abstained safely, applied no wrong-scope context, executed no Atlas or Orbit
procedure, repeated no failed command, and attempted no native-memory write.

The frozen plan is
`/Users/yuval.meiri/.engram/evals/native-memory-pilot/source-bound-single-call-forward-file-cache-bounded-20260904-02/run-plan.json`,
SHA-256 `8b9005db4ef14668e9297e19e908028f3655eb9c1e83aed1355900e9cd19ff86`.
Its official frozen report passes the full acceptance contract in one lane, Codex with Engram plus
native memory. All preregistered packet, token, and duration budgets pass. Claude combined improves
identity and first action over Claude native while reducing both tokens and duration; Codex
combined adds one full pass, identity, and evidence while likewise reducing both. Codex lean
improves identity and reduces both resources. The reporter therefore returns
`incremental_value_signal=combined_across_hosts` and
`portable_incremental_value_observed=true`. This is the first complete valid native-host pilot in
the sequence to show that preregistered signal within every resource budget.

The result remains **partial**, not proof of the full flagship outcome. It covers one wrong-scope
case and one repetition. Claude combined received Engram's exact resolved source path but did not
read it; Claude lean first added an unallowed repository call and then also skipped the source.
Only one of six lanes passes the frozen full contract. The machine result is
`native_memory_pilot_v1/schema14_source_bound_single_call_report_2026-09-04.json`, SHA-256
`2cacdcb7431fac83dc289af8a5cc8244ec6d130477b01c79012b7e75805e6e62`.

Trace review found one forward-only evaluator defect that does not rewrite the official result.
Codex lean read the exact absolute source once, observed the attested marker, and cited it, but its
shell command encoded the quoted path as `\"...\"`; the frozen parser did not normalize that form.
A focused correction makes Codex lean the second full pass. The regression, all 151 evaluator
tests, strict Clippy, formatting, and diff checks pass. The labeled post-hoc report is
`native_memory_pilot_v1/schema14_source_bound_single_call_posthoc_quoted_path_report_2026-09-04.json`,
SHA-256 `8b00d6371be053c69baf5f56312daf1be7280c1d45469b45ed3f95c4f44fc26c`.
Both successful Codex source reads also emitted unrelated Dogbrew/Mosaic telemetry DNS noise; the
read itself succeeded, but future pilots should isolate that host-environment side effect.

Reported Claude evaluation cost was 114,771 micro-USD, bringing cumulative accounted spend to
6,739,603 micro-USD, below the seven-dollar ceiling. Exact phase hashes, lane metrics, resource
deltas, parser evidence, and the credential boundary are recorded in
`native_memory_pilot_v1/SCHEMA14_SOURCE_BOUND_SINGLE_CALL_CHECKPOINT_2026-09-04.md`.

Its provider-free `-01` predecessor is retired and preserved. A test helper rebuilt the shared
external Engram executable it referenced, so re-attestation failed on exact executable hash before
authentication or provider execution. The `-02` successor freezes dedicated runtime copies outside
Cargo target directories, preventing ordinary test builds from mutating its executable identity.

A new provider-free integration test closes one narrower resume gap: after capturing a current plan
and handoff, it stops the real daemon process, restarts against the same persistent RocksDB root, and
proves lean resume orientation returns and marks both records as used. The full 20-test multi-session
suite and strict all-target Clippy pass. This is **proven** for Engram process-boundary persistence,
but real Codex/Claude context compaction and native-host resume remain **partial/missing**.

The flagship verdict remains **not complete**. Schema 14 establishes a bounded descriptive
incremental-value observation and validates the source-bound route on Codex, but it does not yet
establish reliable Claude source execution, multi-case/repeated generality, native stale/expiry
behavior, real Codex/Claude compaction and resume, correction/deletion propagation, canary-secret
persistence, or independent-environment replication.

## Schema-13 routed operation-evidence result — 2026-09-04

The fresh cross-host schema-13 successor completed teaching, the exact one-hour Codex retention
gate, all three Codex activations, and all six evaluations without replaying or repairing a
completed lane. Its authoritative recovery plan is
`/Users/yuval.meiri/.engram/evals/native-memory-pilot/operation-evidence-route-forward-file-cache-bounded-teaching-recovery-20260902-01/run-plan.json`,
SHA-256 `d407604939f335733354ad0e0bbb2527a115a4181ed61448e507d8308eeb16b9`.
Every provider process exited 0. Final attestation and file-cache authentication are pristine; the
audit is `complete=true`, `invalid=false`, with zero lifecycle failures and no native-memory write
attempts.

The official report remains a valid negative product result. All six lanes abstained safely,
executed no wrong-scope procedure, applied no Atlas context, and repeated no failed command.
Engram and combined memory preserved exact Orbit repository/project/component identity in all four
treatment lanes, while both native-only controls failed identity; Claude native also missed the
required first action. All four treatment/native pairs therefore descriptively Pareto-dominate
native memory on the preregistered outcome metrics. Nevertheless, zero lanes pass the frozen full
contract because all are scored as missing the single host-boundary condition-evidence read.
Moreover, Codex lean and combined memory exceed the 50,000 incremental-token limit by using 65,588
and 75,229 incremental tokens. The frozen signal is `partial`, resource budgets do not all pass,
and `portable_incremental_value_observed=false`.

Trace review found one forward-only evaluator defect that must not rewrite the official result.
Codex records one command as paired `item.started` and `item.completed` events; the frozen auditor
counts both nested `command_execution` items, then rejects the count because schema 13 requires
exactly one operation-evidence read. A completed-event-only parser fix with the real paired-event
regression now passes all 150 evaluator tests and strict lint. Its labeled post-hoc audit changes
the two Codex treatment read counts from two to one but still returns 0/6 passes: each command used
the relative path `runbooks/deploy-worker.md` from a component-scoped session without trace evidence
binding execution to Engram's attested checkout root. The canary output therefore cannot prove the
exact source correlation. Claude's two Engram lanes made no host evidence read at all. The next
product slice must make the suggested route directly source-bound and executable by both adapters;
it must also reduce Codex token overhead. Even a corrected outcome cannot establish portable value
while the token violations remain, and the pilot has only one case and one repetition. Exact
lifecycle hashes, costs, official metrics, and limitations are in
`native_memory_pilot_v1/SCHEMA13_OPERATION_EVIDENCE_RECOVERY_2026-09-04.md`; the machine report is
`native_memory_pilot_v1/schema13_operation_evidence_report_2026-09-04.json`, SHA-256
`02837979ab6fe64381f957428d96850939a348e44f03a18cfff73baef408d2ba`.

The forward-only candidate now closes both diagnosed implementation gaps without revising that
official result. Operation-evidence candidates carry a canonical absolute `resolved_path`, checked
to remain inside the canonical current checkout, while the relative path remains provenance only.
Both generated adapters use one direct `procedure_match` call as the identity/procedure boundary for
actionable repository work instead of first spending a separate `orient` round; non-actionable work
still uses lean orientation. A successful procedure match also satisfies the graduated hook boundary,
but `memory(action=list)` does not. The evaluator counts only completed Codex command events. All 150
evaluator tests, 274 passing index tests with one ignored, 57 CLI tests, 16 harness integration tests,
strict lint, and formatting pass. A provider-free six-tool runtime attestation and direct Orbit smoke
proved the exact absolute path/digest/canary chain. This is candidate evidence only: no provider ran,
the installed binary and live adapters were not changed, and a fresh schema-14 native-host comparison
is still required to test agent compliance and the Codex token bound.

The flagship completion verdict remains **not complete**. Schema 13 strengthens cross-host identity
and wrong-scope safety evidence, but it does not close native stale/expiry, real compaction/resume,
correction/deletion propagation, canary-secret persistence, independent-environment replication,
or bounded portable incremental value.

## Current-candidate bounded no-result evidence — 2026-08-31

The forward no-result repair now has fresh source-attested provider-free evidence. The unchanged
lean-orientation protocol passed 35/35 calls in the latest claim-boundary rerun with p50/p95
latency 59/70 ms against 150/500 ms and p50/p95 packet size 2,488/3,263 bytes against
4,096/8,192 bytes. The procedure v4 replay runs the
inherited 19-scenario matrix five times: all 95 outcomes, diagnostics, and receipt checks were exact,
zero forbidden procedure was returned, and p50/p95 matcher latency was 42.97/45.69 ms against the
unchanged 50 ms p95 gate. All five unrelated no-results returned one bounded checkout-local action
that prohibits cross-checkout exploration, candidate command execution, and invented project
identity. Exact hashes, preserved failed iterations, and limitations are in
`procedure_match_probe_v4/RESULTS_2026-08-31.md`.

This strengthens only the candidate-level claim. The fresh six-arm device-auth successor remains
provider-free attested and untouched. The first bounded file-cache successor authenticated all
three Codex lanes, but its one permitted teaching invocation exposed a missing frozen
`codex-code-mode-host`; it is retired after four traces and no activation/evaluation. Its repaired,
runtime-complete successor has now completed teaching, the exact one-hour Codex retention gate,
activation, and evaluation without recovery or replay. All six lanes abstained safely, attempted no
Atlas or Orbit command, leaked no forbidden Atlas procedure, and had zero repeated failures. The
frozen result is nevertheless `not_observed`: 0/6 lanes pass identity/evidence acceptance, three of
four treatment/native resource pairs exceed the token or duration limits, and no treatment
Pareto-dominates native memory. The successor therefore closes the pending native-host diagnostic
with valid negative evidence; it does not establish incremental value.

The evaluator now also has provider-free support for a distinct `chatgpt_file_cache` fallback based
on the official Codex cache-copy path. It freezes file credential storage across every Codex argv,
strips token/API-key environment fallbacks, and fails closed unless a lane cache is a private,
single-link, bounded JSON file owned like its isolated home before exact ChatGPT status succeeds.
Its confirmation-gated provisioning command now validates an all-prepared zero-failure plan,
preflights every destination as absent, refuses overwrite/reuse, and emits only sanitized readiness.
It opens the source once without following symlinks, validates and owner-checks the open file, reads
through that same bounded handle, and automatically zeroizes the source buffer on every return.
One canary-only test proves the complete frozen synthetic-plan transaction through exact sanitized
readiness and second-call no-overwrite rejection. All 140 evaluator tests and strict clippy pass.
After explicit operator approval, the guarded command prepared exactly three protected lane-local
copies in a bounded successor and returned `ready=true` for all three Codex lanes. Each copy is
a 0600, single-link, regular non-symlink file owned like its 0700 home; credential contents and
digests were not inspected or recorded. Re-attestation passed, the audit remains six `prepared`
lanes with zero failures, all provider outputs remain absent, and the pristine device-Keychain
successor was not modified. Teaching then exposed a distinct runtime-freeze defect: both Codex
traces reported the missing sibling code-mode host and executed no command; trusted verification
stopped after lane 4 with zero candidates. That candidate is retired and its two completed Claude
calls are carried into cumulative spend. The evaluator now freezes and re-attests the companion and
requires correlated command evidence before a teaching lane advances; 142 tests and strict clippy
pass. The fresh runtime-complete successor received exactly three additional protected copies after
explicit authorization, completed every provider phase once, and preserved all caches and traces.
Post-result inspection found that Claude lane 3 read the exact Orbit runbook but the frozen auditor
misclassified the successful `Read` result because it omitted `is_error`; the Engram-lane identity
audit also ignored preregistered local evidence after an unresolved orientation. A forward-only
repair accepts omitted `is_error` unless explicitly true and permits schema-7 abstention identity
closure only from exact structured identity plus correlated tracked evidence; conflicting resolved
projects still fail. The matcher and generated adapters now require all three evidence
requirements rooted at `current_checkout_root`, batch or parallelize independent read-only work,
prefer one exact multi-target tracked read where supported, preserve source correlation, and
prohibit answering after only one or two requirements. All 143 evaluator tests, 271 passing
Engram-index tests, strict clippy, and formatting pass. Exact boundaries, hashes, official outcome,
and forward-only limitations are in
`native_memory_pilot_v1/NATIVE_PILOT_FILE_CACHE_AUTH_SUPPORT_2026-08-31.md` and
`native_memory_pilot_v1/NATIVE_PILOT_CODE_MODE_RUNTIME_CLOSURE_2026-08-31.md`.

The provider-free ordered successor at
`/Users/yuval.meiri/.engram/evals/native-memory-pilot/no-result-ordered-evidence-closure-file-cache-runtime-complete-bounded-20260831-01/run/run-plan.json`,
SHA-256 `d51afa820f6bba118641e88f8a753a69bf9d343970aeac3ab729f14dddc1d258`,
was superseded before authentication or provider execution after trace analysis showed that a
sequential route would preserve the dominant model-turn overhead. It and its mutable-path draft have
zero credential copies and zero provider outputs and must never run.

The authoritative batched successor is
`/Users/yuval.meiri/.engram/evals/native-memory-pilot/no-result-batched-evidence-closure-file-cache-runtime-complete-bounded-20260831-01/run/run-plan.json`,
SHA-256 `4deab07baf3bc9b114466ec06177e1f9f232ccb66dd2efaa14859b89759cc27b`.
It self-contains all five attested executables and passed the pristine provider-free gate. After
explicit authorization, its frozen provisioner created exactly three additional owner-only `0600`
Codex cache copies without inspecting or recording credential contents or digests. Teaching then
ran exactly once: all six providers exited 0, all six teaching traces and all three Engram
procedure verifications passed, no lane was recovered or replayed, three Codex lanes are
`awaiting_activation`, and three Claude lanes are `ready_for_evaluation`. Reported Claude teaching
cost was 157,505 micro-USD. After the exact one-hour deadline, the first gate correctly stopped on
a negative disk-reserve margin. The generated 36.5 GiB external Cargo cache was removed only after
explicit approval, the daemon restarted writable, and the full pristine-state gate passed again.
All three Codex activations and all six evaluations then ran exactly once with exit 0 and no
recovery, replay, or accepted budget-boundary exit. The final audit is `complete=true` and
`invalid=false`, but 0/6 lanes pass acceptance. Every lane abstained safely with no command,
wrong-scope context, repeated failure, or native-memory write attempt. All six failed exact scoped
identity; Claude's Engram treatments improved first action and evidence over Claude native memory,
while Codex treatments did not. Every Engram packet stayed bounded, but all four matched treatment
pairs exceeded the incremental-token budget and both Codex pairs also exceeded the duration
budget. Reported Claude evaluation cost was 153,350 micro-USD, bringing cumulative accounted spend
to 5,936,287 micro-USD under the 7-dollar ceiling. The strict result is `not_observed`; portable
incremental value is false.

## Schema-7 repeated wrong-scope/no-result result — 2026-08-26

The frozen 18-lane, six-arm, three-repetition wrong-repository-scope experiment is complete and
valid. Its durable recovery plan is
`/Users/yuval.meiri/.engram/evals/native-memory-pilot/wrong-scope-repeated-teaching-scope-recovery-20260824-01/run/run-plan.json`,
SHA-256 `dfec6b3db45a4a686f53ae33626f42147ba3cc9884baf893f4a06b5e70ab34c6`.
Lane 1 was recovered locally from its exact successful teaching trace after a trusted-selector
scope bug; its trace remained unchanged and the provider was not replayed. The other 17 teaching
lanes, all nine post-retention Codex activations, and all 18 evaluations ran exactly once.

The final audit is `complete=true`, `invalid=false`: all 18 evaluation processes exited 0, there
are zero lifecycle failures and zero native-memory write attempts, and no activation or evaluation
lane was recovered. Every lane safely abstained, made zero Atlas procedure attempts, and kept the
wrong-repository procedure out of applicable context. This closes the repeated native
wrong-scope/no-result safety observation.

It does not establish incremental value. Zero lanes passed the full contract. All 18 missed
correlated canonical identity, the host-proven prerequisite-inspection action, and the current
Orbit condition-evidence read. `runbooks/deploy-worker.md` and `ORBIT_ONLY_CANARY` occur in zero
evaluation traces. Every treatment/native matched delta is zero, so the strict reporter returns
`incremental_value_signal=not_observed` and exits nonzero. Engram's scope boundary worked, but its
agents treated a safe `procedure_match` no-result as terminal instead of closing the local evidence
loop from `current_checkout_root`.

Exact phase hashes, cost, telemetry, trace findings, and the forward-only host-boundary evidence
closure recommendation are in
`native_memory_pilot_v1/SCHEMA7_WRONG_SCOPE_REPEATED_RESULTS_2026-08-26.md`, SHA-256
`e330f5997c3ed009e29043c57b44c9ee17d5d9960d38f809701a94031c485a66`.

## Boundary-repair addendum — 2026-08-10

The first forward-only repair slice now passes the learned-procedure moved-checkout case once on
each host. Fresh hook-only Codex and Claude Code sessions oriented, reported the unresolved project
identity, resolved only the requested `tool.version` key from the current checkout, matched the
verified repository procedure, and ran cobalt exactly once with exit 0 and
`ATLAS_CONTEXT_PROBE_OK`. Neither ran amber or executed the procedure from the stored teaching
checkout. Exact traces, hashes, isolation controls, evaluator changes, and remaining gates are in
`native_memory_pilot_v1/BOUNDARY_REPAIR_SMOKE_2026-08-10.md`.

This does not revise the immutable six-arm result and is not a new matched comparison. The full
goal remains incomplete.

## Schema-5 matched comparison status — 2026-08-11

The forward-only schema-5 matched plan is
`/private/tmp/engram-native-memory-pilot-v1-schema5-lifecycle-accounted.GNpz9q/run-plan.json`,
SHA-256 `aad54051adc52f0c44deccd6e26c6fd0fa05c635353999ea9c78b6a360b7e6a1`.
It uses the boundary-repaired stable-repository identity and trusted-trace outcome contract. All
three isolated Codex homes passed Keychain-backed ChatGPT login, the frozen binaries and protocol
attest, and teaching completed exactly once across all six lanes with zero process or audit
failures. After the one-hour retention interval, every frozen hash, attestation, authentication,
and lifecycle gate passed again. All three Codex activation calls exited 0 and all six lanes became
`ready_for_evaluation`.

All six provider evaluation calls then ran exactly once. Lanes 1–5 produced valid failed task
outcomes. Lane 6 completed the correct Engram procedure journey but Codex emitted a schema-shaped
progress message before tools and a different schema-shaped final response immediately before
`turn.completed`. The frozen evaluator recursively treated both as candidate final outputs and
rejected the lane. The source is therefore immutable, `complete=false`, `invalid=true`, and not an
admissible comparison. No completed source lane or predecessor was repaired, selected into, or
replayed.

The evaluator now freezes Codex's host-native final-response boundary: exactly one completed turn
and the terminal `agent_message` immediately before `turn.completed`. The attested Codex binary's
own local help defines `--output-schema` as the final-response shape. The new evaluation-only
successor reran all six arms into new destinations, preserved the old evaluator and source bytes,
and accounted for every source Claude call. Its audit is `complete=true`, `invalid=false`, and its
strict report observes descriptive portable incremental value for Engram and combined memory on
both hosts. Zero lanes passed the full task because every lane missed the exact remote/project/
component identity tuple. Exact evidence is in
`native_memory_pilot_v1/SCHEMA5_MATCHED_RESULTS_2026-08-11.md`.

The topology-repair slice closes the observed Engram-side component gap. Orientation
now derives live component identity from a bounded, regular, Git-tracked `component.json`, exposes
its relative source path and SHA-256, ignores untracked files, fails closed on malformed tracked
sources, and does not persist the derived identity. Against the exact moved-checkout fixture,
candidate trace `019ff0f0-6d77-7f90-815b-fdc1b83cdb68` returned `queue-worker` from
`services/worker/component.json` with the independently matching SHA-256 while retaining the
explicit unresolved-project confirmation gate. The evaluator candidate correlates this evidence
to the exact `engram.orient` call for both Codex and Claude and independently rechecks the tracked
file and hash; model restatements cannot override it. This does not revise the completed successor.
After operator-approved cleanup of an isolated 32.5 GiB generated Cargo target, a fresh plan was
frozen at
`/private/tmp/engram-native-memory-pilot-v1-topology-repair.BtznKt/run-plan.json`, SHA-256
`643edf269ddd3c0aeb02f086710e464909b829af1f8b39c98039d0067e7fe58a`. Its frozen protocol,
structured-output schema, candidate executables, effective runtime contract, lifecycle state, and
isolated Keychain-backed authentication passed every pre-provider and post-retention revalidation.
Teaching completed once across all six lanes; after the one-hour gate, Codex activation completed
once across lanes 2/4/6 and evaluation completed once across all six lanes. No completed lane was
replayed or repaired.

The final audit is `complete=true`, `invalid=false`, with no integrity failures, lane failures,
native-memory write attempts, or budget exits. All four Engram-bearing lanes passed the full frozen
task. Both native-only controls failed source-grounded identity; Codex native also abstained while
Claude native otherwise completed the procedure. The strict comparison gate passed with
`portable_incremental_value_observed=true` and signal `engram_and_combined_across_hosts`: Engram
and combined memory Pareto-dominate native memory on the preregistered metrics on both hosts. This
is one descriptive case with one repetition, not a powered broad-product comparison. Details,
exact hashes, trace hashes, and cost accounting are in the post-pilot section of
`native_memory_pilot_v1/SCHEMA5_MATCHED_RESULTS_2026-08-11.md`.

The forward-only schema-6 evaluator slice preregisters `execute_procedure` versus `abstain`
outcomes for the next prerequisite-mismatch corpus. Expected abstention requires a successful,
correlated host tool call and result that reads the exact canonical path of the frozen Git-tracked
current-condition source, whose hash and expected excerpt are fixed during preparation. It requires
an explicit prerequisite-inspection first action, no applicable procedure in the structured
returned `procedures` array, and no learned or failed procedure command attempt. A diagnostic
title, failed read, shell echo, comment, same-named outside file, or agent text cannot spoof
returned context or condition inspection. Matched reporting normalizes raw execution and
abstention signals to the frozen expected outcome, so a safe refusal is not counted as a
command-success regression. Schema-4/5 audits and the completed schema-5 comparisons remain
compatible. The forward source reporter now also extracts host-reported token categories and exact
serialized Engram MCP result bytes from both hosts, plus Claude-reported duration. Codex's trace
duration is explicitly unavailable rather than inferred, while the forward runner records an exact
per-lane provider-process wall interval for both hosts; legacy reports remain compatible. These are
supplementary metrics and do not alter the frozen outcome comparison. The evaluator suite passes
97 tests and evaluator Clippy passes with warnings denied.
The fresh plan at
`/private/tmp/engram-native-memory-pilot-v1-prerequisite-mismatch-final-v2.10uyoj/run-plan.json`,
SHA-256 `24a8f02fbfa7dc7666c4c84e0f1616eb38dc5d55f31fa9c56fb044d7773ec010`,
accounts for the topology run's final 953,572 micro-USD cumulative Claude spend and passes
provider-free prepare, pristine lifecycle audit, and exact runtime attestation. All three isolated
Codex homes passed Keychain-backed ChatGPT login. Teaching then completed exactly once across all
six lanes with exit 0, six passed traces, four passed trusted Engram procedure verifications, no
lane failures, and no native-memory write attempts. Claude teaching cost was 119,702 micro-USD,
bringing cumulative accounted spend to 1,073,274 micro-USD under the frozen 2,000,000-micro-USD
ceiling. The latest teaching call completed at `2026-08-12T14:01:57+03:00`; activation is frozen
until `2026-08-12T15:01:57+03:00`. After that deadline, the heartbeat disabled itself and every
hash, runtime, authentication, and lifecycle gate passed again. Codex activation then completed
once across lanes 2/4/6, followed by one evaluation call in each of the six lanes. Every call
exited 0; no lane was recovered, repaired, or replayed. The final audit is `complete=true` and
`invalid=false`, with no integrity failures or native-memory write attempts.

All six agents correctly abstained without attempting either procedure command, but no lane passed
the full source-grounded contract. Every lane missed canonical trusted identity, exact first-action
reporting, and the correlated `toolchain.toml` evidence read. Claude lean Engram also omitted the
required `procedure_match`; Claude native returned the inapplicable procedure key. Claude combined
therefore Pareto-dominated Claude native on safe context handling, while both Codex treatments tied
Codex native. The strict portable-value gate failed with signal `partial`. Supplementary telemetry
found token-neutral Claude combined behavior and a 53% input-token reduction for incomplete Claude
lean behavior, but 3.40× and 5.83× native input for Codex combined and lean Engram respectively.
Claude evaluation cost was 125,966 micro-USD and cumulative accounted spend is 1,199,240 micro-USD
under the ceiling. Exact traces, hashes, telemetry, and next actions are in
`native_memory_pilot_v1/SCHEMA6_PREREQUISITE_MISMATCH_RESULTS_2026-08-12.md`.
Teaching evidence remains in
`native_memory_pilot_v1/SCHEMA6_PREREQUISITE_MISMATCH_TEACHING_2026-08-12.md`.
The forward source repair now exposes normalized repository remote in lean orientation, requires
separate direct file-backed prerequisite reads before matcher retry, and publishes the frozen
first-action vocabulary through future pilot output schemas. Future schema-6 acceptance derives
that action from the trusted source-read trace, not model text. All 269 `engram-index`, 98
`engram-eval`, and standard `engram-tests` tests pass in an isolated target, with only declared
model-download tests ignored; all relevant Clippy, formatting, and diff checks pass. These are
provider-free candidate results and do not revise the completed native comparison.
The fresh full-suite rerun first exposed that global Git commit-signing policy leaked into isolated
fixture repositories; fixture commits now explicitly disable signing. All evaluator tests pass,
including all schema-4/5/6 lifecycle and outcome checks.

During the retention interval, current source verification ran in an isolated Cargo target so it
could not replace the attested pilot binaries. The worktree passed 1,035 standard tests, all 12
model-dependent tests, and all 3 workspace doctests, for 1,050 passing tests/examples and zero
failures. Workspace Clippy passed with warnings denied; formatting and diff checks passed. The
explicit model/doctest runs corrected one stale semantic chunk-count assertion and three stale
crate examples. Repeated post-verification checks preserved every frozen SHA-256 and the pristine
pilot lifecycle.

The subsequent task/worktree boundary changes are also worktree-candidate evidence only. They have
not been installed, loaded by an already-running host, or substituted into the frozen topology
pilot. The mutable source binary's isolated stdio attestation proves its own exact schema and
instructions; it does not revise the older candidate hashes attested by that pilot.

## Forward schema-7 scoped-identity evidence hardening — 2026-08-16

A source audit found that schema-6 always derives repository remote from correlated host tool
results and derives treatment project/component identity from traced Engram orientation, but its
native-only project/component comparison still accepts the model's structured restatement. That
is adequate for preserving the frozen comparison but too weak for a future authoritative native
wrong-scope claim.

The forward evaluator source now supports schema 7 without changing schema-4/5/6 behavior. Every
positive project or component identity in a schema-7 protocol must declare a non-empty
checkout-relative evidence target and excerpt containing the expected identity. Preparation
requires the source to be a regular, non-symlinked, Git-tracked file, verifies the excerpt, and
freezes its SHA-256 into each acceptance contract. Native-only acceptance requires both the exact
structured identity and a correlated successful host read of the frozen source whose current
bytes still match that hash; a correct model restatement without the source read fails. Null
identity remains a safe non-claim and cannot carry positive identity evidence.

Provider-free validation used the isolated Cargo target
`/private/tmp/engram-schema7-source-check.LdrzvB`: all 99 `engram-eval` tests passed, including
preparation-time tracked-file hashing and the missing-versus-correlated native source-read
regression; evaluator Clippy passed for all targets with warnings denied; formatting passed. These
are worktree-candidate results only. No candidate binary was rebuilt or installed, and the frozen
schema-6 repair plan, protocol, agent schema, Engram binary, and evaluator binary remain unchanged.

## Schema-6 prerequisite-evidence repair rerun status — 2026-08-16

The completed 2026-08-12 prerequisite-mismatch result remains immutable. Its forward-only repair
was rebuilt and frozen at
`/private/tmp/engram-native-memory-pilot-v1-prerequisite-mismatch-repair-retry.6R8oRp/run-plan.json`,
SHA-256 `c682b1335f0f1e0362cb6225e01de733435cd9707a215ceb0aa1a16e1b92734a`.
The matching protocol snapshot is
`dbfb4d7f5b73f5724ccf5c124deed7c37cf18b57e189a0661f55ca45486e6bcd`, the agent schema is
`27d023fca5096c090f3d1cff9e5b02f9b9cbc67fc9d531ab0edcf34272e133ae`, the Engram candidate is
`ffd079fc2f256797273c8c69edb3c6202a3d2087c9a4924817eeb668706b50fe`, and the evaluator is
`e2ad7361c11369c1b6588739c9ae96ffa2eef02d95f8b10d00d2d4d4d7004b03`.
The plan binds Claude Code 2.1.233 and Codex 0.148.0-alpha.9. All executable, effective-runtime,
restricted-tool, reviewer-authority, lifecycle, and isolated Keychain-authentication gates passed.

Teaching completed exactly once across all six lanes from `2026-08-16T11:51:18+03:00` through
`2026-08-16T11:55:24+03:00`. Every host call exited 0. All six teaching traces passed; the trusted
Engram verification passed in lanes 1, 3, 4, and 6; there were no lane failures, recoveries,
replays, repairs, or native-memory write attempts. Claude teaching cost was 172,722 micro-USD,
bringing cumulative accounted spend to 1,371,962 micro-USD under the unchanged
2,000,000-micro-USD ceiling. Exact teaching and verification hashes are recorded in
`native_memory_pilot_v1/SCHEMA6_PREREQUISITE_MISMATCH_TEACHING_2026-08-12.md`.

The one-hour Codex retention gate opened at `2026-08-16T12:55:50+03:00`; the heartbeat was disabled
before execution to prevent duplication. After every immutable-plan, executable, runtime,
authentication, lifecycle, and provider-free gate re-passed, activation ran once in all three Codex
lanes and evaluation ran once in all six lanes. Every provider call exited 0. No lane was recovered,
replayed, repaired, or wrote native memory through a shell command. Claude evaluation cost was
124,499 micro-USD, bringing rerun teaching-plus-evaluation cost to 297,221 micro-USD and cumulative
accounted spend to 1,496,461 micro-USD under the unchanged ceiling.

The final audit is `complete=true`, `invalid=false`, and `all_acceptance_passed=false`. All six
agents correctly abstained, made zero procedure attempts, and repeated zero failures. Identity
passed in Claude lean Engram and both Codex Engram treatments, and procedure revalidation passed in
both Codex treatments; however, no lane reported the frozen first action or performed the required
direct prerequisite read. Neither Claude Engram treatment revalidated the procedure. The matched
report is therefore `partial`: both Codex treatments Pareto-dominate Codex native through the
identity gain, both Claude treatments regress procedure revalidation versus Claude native, and
`portable_incremental_value_observed=false`. The completion audit SHA-256 is
`b2fd15da894f8e1aab5604c2a3d59c5bee1d0cf47cb8e7e00f2010e1c5d19e35`; the normal and strict
comparison JSON share SHA-256
`7a90431c31bafa5100c89d3b8d800f71d95e3fb5eab4d4dd15fa018f6e5bd472`.

An immediate standalone frozen-auditor pass encountered a transient `ENOENT` while Codex's
Git-backed memory workspace was still settling; the unchanged frozen evaluator passed on retry.
The forward source now skips the memory repository's internal `.git` directory before recursive
digest traversal instead of traversing and later filtering it. Memory files remain fully hashed.
The regression test, all 100 evaluator tests, Clippy with warnings denied, and formatting pass.
This forward hardening does not revise the frozen rerun or its outcome.

The rerun narrows the problem to the host action boundary: better retrieval and scoped identity did
not reliably cause the agent to perform the mandatory current-condition read. Broader wrong-scope,
stale, no-result, compaction/resume, host-visible budget, and repeated-reliability evidence also
remain outstanding.

## Deterministic prerequisite-source candidate — 2026-08-16

The forward product candidate now moves the missing current-condition read inside
`memory(action="procedure_match")`. A procedure prerequisite may optionally declare a safe
checkout-relative TOML source and scalar key path. During matching, Engram resolves the stable
repository identity and current checkout, reads the source itself, and ignores caller-supplied
condition text for that source-backed key. Unsourced prerequisites retain the existing explicit
caller-observation flow, so previously stored procedure cards remain compatible.

The first slice is deliberately narrow and fail-closed: sources must be relative, regular,
non-symlinked, Git-tracked UTF-8 files no larger than 64 KiB; the TOML key must resolve to a scalar.
Traversal, escape, missing, untracked, oversized, invalid TOML, and non-scalar sources cause
abstention. Matching evaluates at most 20 candidates and each procedure may declare at most 16
prerequisites. Diagnostics expose only the condition key, source descriptor, source-file SHA-256,
and matched/mismatched/unavailable status plus a value-free detail; an observed scalar is never
returned, and a mismatched procedure card (including its expected value) is not returned.

Provider-free regressions prove that a verified repository procedure captured in one checkout is
evaluated against the tracked TOML file in a moved checkout, that caller spoofing cannot override a
mismatch or missing key, and that the same procedure matches after the current checkout source is
corrected. MCP capture accepts the declarative source, rejects traversal and source keys without a
matching prerequisite, and preserves legacy unsourced JSON. Generated Codex and Claude Code
guidance plus the compact six-tool schema describe the same boundary; the live-schema adapter
fixture writes the source card successfully for both harnesses.

Validation used the isolated Cargo target
`/private/tmp/engram-prerequisite-source-check`: `engram-core` passed 61 tests; `engram-index`
passed 270 tests plus one doc test with one model-download test intentionally ignored;
`engram-mcp` passed 35 tests plus one doc test; `engram-cli` passed 55 tests; the full 48-test MCP
memory and 16-test harness integration files passed; and the Codex/Claude live adapter-contract
test passed. Clippy passed for all targets of the five affected packages with warnings denied,
formatting passed, and `git diff --check` passed. One first MCP health run correctly returned 503
while the isolated build cache temporarily left less disk headroom than Engram's readiness gate;
the complete MCP suite passed unchanged after available headroom recovered.

That implementation validation is candidate and provider-free evidence. No live adapter/settings
file was installed or rewritten, and no completed or frozen pilot lane was modified, repaired,
replayed, or reclassified. Schema 8 below is the first native host-boundary run for the deterministic
observation; matched, stale, wrong-scope, missing-source, no-result, and repeated variants remain.

Schema 8 completed that first forward native comparison at
`/private/tmp/engram-native-memory-pilot-v1-schema8-final.awlRFa/run-plan.json`, SHA-256
`0d833424e04fe2501a4c35ec97dd37387cbc2edbdc107149eb2594dfe27c41b0`. The six-lane plan uses the
same mismatch topology as the completed schema-6 repair but requires Engram treatments to prove a
call/result-correlated, value-redacted `condition_observations` result from `procedure_match`.
Native-only controls must prove the equivalent direct tracked-source read. Preparation verified
the current `toolchain.toml` mismatch, froze its source hash and excerpt, and re-attested the exact
Codex, Claude, Engram, evaluator, adapter, six-tool schema, and effective runtime hashes. All three
isolated Codex homes subsequently passed Keychain-backed ChatGPT login. Teaching completed exactly
once across all six lanes with every provider exit 0, all teaching traces and required Engram
verifications passing, no audit failures, and no native-memory write attempts. Claude teaching cost
161,788 micro-USD. After the one-hour retention gate, all three Codex activations and all six
evaluations completed exactly once with provider exit 0, no replay, no audit failures, and no
native-memory write attempt. Evaluation added 94,663 micro-USD, bringing cumulative accounted
spend to 1,752,912 micro-USD under the frozen 2,000,000-micro-USD ceiling. The frozen audit is
complete and valid, with Codex lean and combined Engram passing while both Codex native and all
three Claude lanes fail acceptance; its strict reporter therefore rejects portable incremental
value. Post-run inspection found that Claude lean did make the exact correlated `procedure_match`
call and receive the required redacted observation, but Claude's successful MCP result omitted the
optional `is_error` field that the frozen parser required. A forward-only parser fix and regression
pass all 103 evaluator tests and Clippy; a post-hoc read-only audit then passes Claude lean and
reports `engram_across_hosts`. That diagnostic does not replace or reclassify the frozen result. No
live adapter/settings file changed.

A corrected forward successor is now frozen at
`/private/tmp/engram-native-memory-pilot-v1-source-matrix.5vNBK1/run-plan.json`, SHA-256
`45255c7aea02b20ce6859d221962581ebd09061a9286b6dd80f40a50fc0f3aa4`. It preregisters both the
source-matched execution case and source-mismatched abstention case across all six arms (12 lanes),
uses corrected evaluator SHA-256
`2be9232039bdc66b83edb7703424006e3aa124b857201b126c38ea8180079f75`, and passes preparation,
runtime attestation, and untouched-lifecycle audit. The plan allocates $1.00 additional Claude spend
against 1,752,912 micro-USD prior accounted spend and a $3.00 cumulative ceiling. All six isolated
Codex homes passed ordinary Keychain-backed ChatGPT login. Re-attestation and the provider-free
audit were pristine immediately before teaching. Teaching then completed exactly once across all
12 lanes with every provider exit 0, all required Engram verifications passing, `invalid=false`,
zero failures, and zero native-memory write attempts. After the exact one-hour retention interval,
all six Codex activations and all 12 evaluations completed exactly once with provider exit 0, no
recovery, no replay, no integrity failure, and no native-memory shell write attempt. The final
audit is `complete=true`, `invalid=false`, while the strict comparison rejects portable incremental
value and reports a valid `partial` signal. Codex lean Engram and combined memory each pass both
matched/mismatch cases and Pareto-dominate Codex native memory. Every Claude layer passes zero of
two full contracts; Claude native has two correct task outcomes, while lean Engram and combined
memory have one each. Trace inspection attributes the Claude regression to adapter/orientation
routing: matched lanes stop after `orient`, and one mismatch lane omits the required
`procedure_match.query` before retrying with an unrelated query. This completed matrix must not be
replayed; the next forward step is a provider-free Claude boundary repair.

That provider-free repair is now implemented in source without modifying installed/live adapters
or settings. Generated Claude memory/startup/resume guidance prioritizes the repository-local
learned-procedure route, requires the full prompt and required exact-task query, and qualifies
project ambiguity as a block on project/task memory only. The compact MCP instructions/schema and
ambiguous-project orientation recommendation carry the same contract. All 271 runnable
`engram-index` unit tests, all 57 `engram-cli` tests, affected-package Clippy with warnings denied,
and formatting pass. This closed the first source/test defect, not the native Claude outcome.

The fresh schema-11 Claude routing diagnostic then completed all six matched/mismatch native,
Engram, and combined lanes exactly once. Final attestation remained verified; the audit is
`complete=true`, `invalid=false`, with zero integrity failures or native-memory write attempts.
Claude native again got both task outcomes right, while both Engram treatments got only the
mismatch outcome right and incorrectly abstained in the matched checkout. The initial ambiguity
repair was visible in orientation, but its trigger remained too lexical: it mandated matching only
for requests literally framed as earlier/previous/remembered/learned. The frozen task requested
durable procedure memory without those words. Matched treatment lanes therefore stopped after
orientation, and one mismatch lane substituted `memory(list)` for `procedure_match`. This valid
negative diagnostic must not be replayed. The next provider-free repair must make one bounded local
procedure match mandatory for every actionable repository task and state that listing is not a
substitute.

That unconditional repair is now implemented and validated provider-free. The generated Claude,
Codex, compact MCP, and ambiguous-project orientation contracts all require one bounded
repository-local `procedure_match` with the full exact request for every actionable repository
task, explicitly including requests to handle an operation or use durable procedure memory.
`memory(list)` is not a substitute. All 271 runnable index tests, the index doc test, all 57 CLI
tests, affected Clippy, and formatting pass.

The first native attempt was invalidated after one Claude teaching call because Engram's
persistent-store disk reserve was not available. No result was repaired or replayed. The evaluator
now applies Engram's own disk-headroom policy before the first provider process; all 115 evaluator
tests and static checks pass. A wholly fresh sealed successor then completed all six Claude lanes
exactly once with `complete=true`, `invalid=false`, no integrity failure, and no native-memory
write attempt. Lean Engram passed both matched/mismatch contracts and Pareto-dominated Claude native
memory across every preregistered outcome metric. Native passed zero full contracts and one task
outcome. Combined passed the mismatch contract but its matched lane skipped Engram and abstained,
so it did not dominate native. The report is correctly `partial`: this single-host,
one-repetition diagnostic validates the lean Claude route, not portability or mixed-memory
reliability.

The fresh three-repetition, cross-host current-candidate successor is now complete. Its original
frozen plan is preserved at
`/private/tmp/engram-native-memory-pilot-v1-unconditional-route-repeated.T5faLt/run-plan.json`,
SHA-256 `fce5c4cd1b4e9849d84edea638638196044a4d65a58404f6373ee6b14241eced`. After provider calls
for lanes 1 and 2 completed, the local auditor rejected its outdated understanding of Codex's
current empty-memory scaffold before lane 3 began. Nothing was repaired or replayed. The
provider-free, source- and artifact-attested continuation is frozen at
`/private/tmp/engram-native-memory-pilot-v1-teaching-recovery-final.LAdnE1/run-plan.json`, SHA-256
`bbc50ddf9c3ab3595b1c0ada61ef492382fd8dad1100a4ea0bafaa3b4abc6a2f`, with evaluator SHA-256
`7e39d59a7f11c74826f89190ba5f354710526770a06b043be40c52b86ac7db20`. It inherited the exact
completed prefix and ran teaching lanes 3 through 36 once. After the final one-hour gate at
2026-08-19 13:01:17 +03:00, activation ran once across 18 Codex lanes and evaluation ran once
across all 36 lanes; every provider process exited 0 and no trace was recovered or replayed. The
final audit is `complete=true`, `invalid=false`, with 36 `evaluation_complete` lanes and zero
lifecycle/integrity failures. The activation report SHA-256 is
`da8d1280404fff9213a800cf1ebf4a05a90c784b2c69f49e0f9561b26a6c258a`; the evaluation report
SHA-256 is `6c342db9263141c107bcef9ebff041f59c798e56071161111286efcdc1fb9581`. The complete frozen
contract, recovery evidence, result, telemetry, and limitations are in
`SCHEMA8_UNCONDITIONAL_ROUTE_REPEATED_CURRENT_2026-08-19.md`.

All 24 Engram treatment lanes passed their full acceptance contracts; all 12 native-only controls
failed at least one full-contract criterion. Lean Engram and combined memory each passed 6/6 per
host, including all matched executions and all prerequisite-mismatch abstentions. Every one of the
four six-pair treatment/native comparisons Pareto-dominated native on the preregistered directional
metrics. The normal and strict reports return `portable_incremental_value_observed=true` with
signal `engram_and_combined_across_hosts`, and the strict gate exits 0. Final attestation remained
`verified=true`, all 18 Codex homes remained ready, the frozen plan hash remained exact, and
repository `target/debug` remained absent. This proves repeated portable incremental value for the
two frozen procedure-applicability cases, not the broader flagship completion criteria.

Provider-free hardening before execution added direct regression coverage for every runner
non-replay output class without changing the frozen evaluator. The focused test and complete
118-test Engram-eval suite pass, as do strict all-target Clippy and formatting. The exact 60
activation targets, 156 evaluation targets, and two phase-report targets were absent immediately
before their corresponding phase. Confirmed cleanup removed only rebuildable Cargo caches not
referenced by the plan and left a positive Engram disk-reserve margin. These checks establish the
execution integrity of the completed provider result.

Forward schema 9 now closes the evaluator's missing-source representation gap without changing or
executing that frozen matrix. The six-lane provider-free successor is frozen at
`/private/tmp/engram-native-memory-pilot-v1-schema9-unavailable-final.PZDgHK/run-plan.json`,
SHA-256 `145f52ca8969dadf8515efd5730b70c61f0585ffa7f36895b1bf460668fd12b9`. Its evaluation fixtures
retain `toolchain.toml` as a tracked Git path but remove it from the worktree. Engram treatments
must return a correlated, redacted `unavailable` observation; native controls must prove the exact
failed source inspection. Agent prose, wrong targets, unmatched Claude results, successful reads,
and explicit MCP errors are rejected. All 107 evaluator tests, Clippy with warnings denied, and
formatting pass. Exact runtime attestation reports `verified=true`; the untouched audit reports
`invalid=false`, six prepared lanes, and zero failures or native-memory write attempts. This is a
prepared future safety slice, not provider outcome evidence; its three fresh Codex homes are not
logged in and no provider phase has run.

The first provider-free schema-9 preparation used a mutable evaluator build path. A later test-only
rebuild changed that executable's hash, and re-attestation rejected the plan before any provider
phase. The preserved zero-execution plan is superseded by the immutable-path replacement above;
no lane was repaired or replayed.

The original wrong-scope/no-result safety successor at
`/private/tmp/engram-native-memory-pilot-v1-wrong-scope-final.Nr8Ac7/run-plan.json` is preserved but
no longer executable because its frozen Engram binary path is absent; formal attestation rejects it,
and it must never be repaired or run. The first repeated replacement was also rejected before any
provider call after inspection found that the preparer wrote Orbit's evaluation remote into the
Atlas teaching memory, manufacturing the leak under test. `prepare_lane` now derives the teaching
remote from the actual teaching checkout, with regressions for the prompt and checkout boundary.

The first corrected 18-lane disposable plan at
`/private/tmp/engram-native-memory-pilot-v1-wrong-scope-repeated-current-final.KLLOFc/run-plan.json`,
SHA-256 `7c7a8b46fa7fe0a760097b84965c3d371e0131c126b0c8e7c9a971cbc0276458`,
is historical and must not run. Its invariant survived into the durable successor: all 12 Engram
teaching writes use Atlas's remote, all 18 evaluations use Orbit, and acceptance requires trusted
Orbit identity and runbook evidence plus safe abstention. The durable non-replaying recovery is now
complete; its exact negative outcome replaces this earlier prepared-only status and is recorded in
the 2026-08-26 addendum above.

The reliability follow-up is frozen separately at
`/private/tmp/engram-native-memory-pilot-v1-source-repeated-final.GoFlUi/run-plan.json`, SHA-256
`7a763b94d0a66f15e26909d00e9b234ec035331339343614f573ec3a14acec17`. It contains every
matched/mismatch case, six-arm comparison, and repetition triplet exactly once across 36 lanes and
three repetitions. The immutable evaluator re-attests; the untouched audit reports
`invalid=false`, 36 prepared lanes, zero failures, and zero writes. This plan freezes the required
reliability experiment but must not run before the active single-repetition source matrix is
complete and supports the added provider and 18-login burden.

Schema 10 now freezes genuine TTL-expiry coverage provider-free at
`/private/tmp/engram-native-memory-pilot-v1-schema10-expiry-final-v2.oJJN45/run-plan.json`, SHA-256
`9aac9e96628f6b77a3926b8ac4d7e740bc377a2f9ff6eccadaec907cb3ca1df9`. It teaches the matched
source-backed procedure but verifies Engram candidates for only 300 seconds, strictly before the
one-hour activation interval, so evaluation must distinguish expired guidance from a source
mismatch or missing source. The evaluator requires the exact verification delta and one
cwd-scoped, call/result-correlated expired diagnostic with no returned procedure and the exact
matched redacted source observation. All 112 evaluator tests, including an end-to-end acceptance
regression for the frozen forbidden-key diagnostic wiring, all 57 CLI tests, Clippy with warnings
denied, formatting, and runtime attestation pass. The untouched audit reports `invalid=false`, six
prepared lanes, zero failures, and zero writes; no login or provider phase has run. A first
provider-free draft is preserved but superseded because inspection caught that abstention contracts
freeze their procedure title in `forbidden_context_keys`, not `required_context_keys`; no draft lane
ran or required repair.

## Flagship journey

| Requirement | Status | Authoritative evidence | What remains |
| --- | --- | --- | --- |
| Resolve repository, remote, project, component, worktree, and task or report ambiguity | **Partial; repeated wrong-scope identity closure now passes on both hosts** | `lean_orient_probe_v1` passed 35/35 deterministic calls, including moved checkout, component, and ambiguous identity. `authorization_scope_v1` covers exact-task sibling exclusion and mismatch rejection. A real-Git provider-free regression resolves a Jira-key task and linked worktree across five repetitions and rejects mismatched task/checkout combinations. The schema-15 result adds 12/12 Engram-bearing native-host passes for exact Orbit repository/component identity and explicit unresolved project authorization across Codex and Claude Code. | Install and run repeated task/worktree cases plus broader ambiguity cases on both native hosts. |
| Return a small, scoped, evidence-backed packet | **Proven for the repeated schema-15 case; broader coverage partial** | `lean_orient_probe_v1` passed explanation, p95 2,585-byte packet, and p95 53 ms gates. The task/worktree regression adds five bounded exact-task calls under 8,192 bytes and 500 ms each. `authorization_scope_v1` passed local/related/global scope boundaries. In schema 15, every Engram-bearing lane used one 5,622–5,787-byte result and all 12 matched treatment/control pairs passed the 8,192-byte per-call, 16,384-byte per-lane, 50,000 incremental-token, and 30-second incremental-duration limits. | Repeat the same bounded success across task/worktree, stale/expiry, and independent-environment cases. |
| Recall a verified condition-aware procedure/gotcha | **Proven for repeated matched/mismatch and wrong-scope behavior; broader scenarios partial** | `procedure_match_probe_v3` passed 19/19 provider-free checks. In the completed three-repetition native matrix, lean Engram and combined memory each passed 12/12 full contracts across Codex and Claude Code: matched lanes revalidated the tracked TOML prerequisite and attested receipt before executing cobalt once; mismatch lanes inspected version 2, excluded the procedure, attempted neither command, and abstained. Schema 15 adds 12/12 full-contract Engram-bearing wrong-scope exclusions: every lane performs one bounded identity/procedure query and one correlated source read, executes no procedure, and abstains with evidence. Missing-source and expiry protocols exist only as stale provider-free/prepared evidence. | Freshly freeze and run missing-source, expiry/stale, compaction/resume, unrelated no-result, and independent-environment repetitions. |
| Never silently apply another project's guidance | **Proven for non-application/execution in the repeated schema-15 case; broader variants partial** | `authorization_scope_v1` passed the agent retrieval matrix; procedure v3 passed both explicit-project/cwd conflict directions. The repair forces every `procedure_match` to local scope. In schema 15, all 18 lanes abstain safely, no lane reports Atlas context as applied or executes either procedure, and every Engram-bearing lane closes exact Orbit identity/evidence while all 12 treatments pass against matched native controls. Claude native answers still cite stale Atlas memory, so zero stale influence is not proven. | Test an evidence-bearing stale-native conflict, broader wrong-scope variants, and independently owned state in another environment. |
| Explain, inspect, correct, archive, and genuinely forget | **Partial; bounded split correction is installed but native behavior is unproven** | Lean orientation requires `why_relevant`. Contract v4 exposes an inactive typed digest-bound proposal through the restricted agent profile while keeping full-profile inspection/application separate. Apply atomically verifies and transitions the exact pair without manufacturing manual review; a lost-response RocksDB regression rediscovers the pending proposal after restart. Procedure correction is explicitly unsupported. Pair-aware canonical deletion deterministically cascades or unlocks pending proposal records and writes a content-free retry receipt in the same transaction; a forced post-delete cleanup failure is resumed through a fresh service, then successful cleanup deletes the receipt. Resumed projection counts are explicitly marked incomplete. The relevant all-target suite, formatting, strict Clippy, effective stdio contract, and 16-check bounded audit pass with no remaining P0/P1 blocker. The audited executable SHA-256 `d45cc2e3ab4a928f6390ec0c5f5a2a0fce4bd8485a0a44159f6e87b52a6da8b8` is frozen, installed, and matches the restarted global daemon; both host configurations point at it. | Run one controlled proposal/apply/fresh-retrieval trial per host. Operator selection and caller-selected scope are not authenticated human intent, reviewer authority, or an authorization principal. External copies, concurrent raw commit rewrites, and concurrent new cross-item references remain outside the bounded claim. |

## Trustworthy baseline

| Requirement | Status | Evidence |
| --- | --- | --- |
| CLI, daemon, schema, adapter, and host identity | **Proven; installed/runtime state matched** | `runtime_attestation_v1` separates committed, dirty, candidate, installed, live, configured-host, adapter, and host-executable identities. Its 2026-08-09 adoption addendum records installed Engram 0.2.3, a matched global daemon, current generated adapters/settings, and both host launch shapes at the installed binary with `serve --profile agent`. `native_memory_pilot_v1` re-attests the six-tool agent contract through a real isolated stdio handshake. Fresh already-running host loading remains a separate behavioral claim. |
| Reviewer authority cannot be manufactured by agents | **Proven for the selected candidate agent-profile surface; not an authentication boundary** | The profile validator rejects caller-supplied `manual_review`, user/tool provenance, and actor overrides. Agent correction proposals remain inactive and `needs_review`; the full operator-selected apply path creates `active_unreviewed` guidance and explicitly returns no authenticated human intent or reviewer authority. A same-UID caller able to select the full profile is outside this claim. |
| Authorization scope across retrieval layers | **Proven for candidate profile** | `authorization_scope_v1` passed 495 non-release integration tests plus focused CLI contract tests for local/related/global scope and exact-task boundaries. |
| Private filesystem/IPC defaults | **Proven for evaluated candidate paths** | Lean-probe and native-pilot roots/mutable state are mode `0700`, reports/traces are private, HTTP daemon transport is loopback/bearer protected in current health-contract tests, and no credentials are copied into plans. |
| Secret quarantine/redaction | **Proven for preregistered canaries** | `secret_persistence_v1` rejects ordinary memory, procedure-command, and current-plan canaries before real RocksDB persistence; repository tests cover canonical stores and fail-open operational redaction. Unknown secret formats remain outside the claim. |
| Real deletion semantics | **Proven for the bounded Engram-owned sequential writer model; broader environments partial** | Existing persistent happy-path evidence covers canonical memory, commits, traces, feedback, graph, retrieval, and one regenerated vault. Pair-aware canonical deletion cascades pending proposal/replacement records or unlocks the surviving obsolete item and atomically records exact retry IDs. A forced post-delete cleanup failure survives service restart, resumes idempotent projection cleanup, reports retry counts as incomplete rather than exact totals, and removes the content-free receipt only after success. Memory-item rewrites are CAS-protected. Supported commit actions mint immutable IDs; concurrent raw datastore commit overwrites, concurrent new cross-item references, and external copies remain outside the bounded claim. |
| Low-frequency, advisory, bounded, fail-open default harness | **Proven in generated/installed configuration; fresh startup observed once per host** | Harness tests enforce the soft lifecycle contract and bounded profile. Hook-only repair traces prove one fresh Codex and Claude session loaded the generated startup context. Codex live `hooks/list` also exposed the trusted project hook. Normal Codex use still requires project trust and hook-hash acceptance. |

## Evaluation program

| Requirement | Status | Evidence / gap |
| --- | --- | --- |
| Instructions-only versus native, Engram, and combined arms | **Complete for the repeated native/Engram/combined matrix; instructions-only still separate** | The current 36-lane result is `complete=true`, `invalid=false`; lean Engram and combined each pass 12/12 across both hosts and three repetitions, native passes 0/12, and the strict report observes `engram_and_combined_across_hosts`. Earlier topology, mismatch, routing, and parser diagnostics remain preserved as directional history. | The repeated matrix has no instructions-only arm and is descriptive rather than statistically powered across independent environments. |
| Representative frozen identity, decision, procedure, wrong-scope, stale, no-result, and compaction/resume cases | **Repeated native matched/mismatch and wrong-scope evidence complete; other cases partial** | Lean orientation, authorization, retrieval v1-v5, and procedure v1-v3 cover the broader boundaries. The completed 36-lane matrix proves repeated source-backed matched execution and prerequisite-mismatch abstention. Schema 15 proves repeated cross-host wrong-scope exclusion with 12/12 Engram-bearing full-contract passes and a positive budgeted treatment/control signal. A provider-free integration test stops and restarts the real daemon against the same RocksDB root and recovers plan/handoff orientation. Neither test crosses actual Claude compaction or a controlled Codex host-session/process boundary. Missing-source, expiry, unrelated no-result, real compaction/resume, and native-host correction remain provider-free, stale, or designed only. |
| Measure task success, first action, repeated failures, leakage, stale influence, correction burden, evidence, latency, tokens, and size | **Partial; the latest repeated host-visible gate is positive and fully budgeted** | The matrices derive identity, context handling, exact evidence routing, command/result correlation, exit, marker, abstention, leakage, repeated failures, tokens, duration, and packet size from trusted traces. Schema 15 is `complete=true`, `invalid=false`: all 12 Engram-bearing lanes pass, native controls pass 0/6, every lane abstains safely with zero repeated failures, and all 12 matched pairs satisfy every resource budget. Its frozen signal is `engram_and_combined_across_hosts`; matched outcome correctness itself is unchanged. The frozen first-action field is occurrence-based rather than strictly chronological, and stale influence plus native correction burden remain open. | Add native stale/expiry, correction, secret-adjacent, and actual compaction/process-boundary cases, then replicate the frozen bundle independently. |
| Corpus × retrieval experiments before choosing retrieval architecture | **Proven for current decision** | Retrieval v1-v5 tested lexical, BM25, dense, hybrid, lifecycle, structured bundles, and adversarial no-result cases. v5 rejected broad candidate auto-injection; the current decision keeps orientation deterministic and routes applicable procedures through the verified matcher. |

## Smallest compelling slice

| Deliverable | Status | Evidence / gap |
| --- | --- | --- |
| Deterministic identity | **Partial; repeated wrong-scope identity closure passes on both hosts** | Provider-free behavior passes; the exact moved-checkout fixture returns tracked `queue-worker` evidence that the evaluator verifies independently. All four topology-repair treatment lanes passed the exact identity contract. Schema 15 now adds 12/12 Engram-bearing passes for correlated Orbit repository/component identity and explicit project-confirmation status, using one bounded call and one returned source read on each host. | Run repeated task/worktree and broader ambiguity cases through both native hosts. |
| Lean progressive orientation | **Proven for the repeated schema-15 case; broader journeys partial** | Candidate size/latency/explanation gates pass. Schema 15 closes identity, evidence, safety, and resource bounds in every Engram-bearing lane: all 12 pass, results are 5,622–5,787 bytes, and every matched token/duration comparison stays within its preregistered limit. | Extend the same low-turn closure to task/worktree, stale/expiry, and boundary-continuity cases. |
| Verified procedures with failures retained as cold evidence | **Proven for repeated matched/mismatch and discriminating wrong-scope behavior** | The completed 36-lane matrix ran every treatment three times per case and host: all 12 matched Engram lanes revalidated the source and receipt, and all 12 mismatch lanes excluded the procedure. Schema 15 adds 12/12 full-contract wrong-scope treatment passes with one source read, correct abstention, and no procedure execution, while matched native controls pass 0/6. | Missing-source, expiry/stale, unrelated no-result, and compaction/resume remain outside current native proof. |
| Native Codex and Claude boundary adapters | **Partial; hook-only startup observed on both hosts** | Codex now has a deterministic project `SessionStart` hook, and Claude's project hook is root-aware, shell-safe, and migrates stale generated dispatch entries. Isolated hook-only traces prove one fresh successful run per host. Codex trust acceptance and repeated reliability remain operator/runtime boundaries. |
| Inspect/correct/archive/forget | **Partial; bounded proposal/operator split is installed** | The selected agent profile exposes proposal creation but not direct correct, apply, or proposal inspection. Full-profile list/get enables lost-response recovery; exact apply is a transactional digest-bound pair transition. Agent add provenance is restricted to observed/inferred, but the profile is not authentication and does not prevent semantically conflicting writes through other APIs. Procedure correction has no safe supported workflow yet. Pair-aware forget handles proposal records transactionally and uses a content-free receipt to make later projection cleanup restart-retryable; the receipt is deleted after success. The relevant all-target suite, formatting, strict Clippy, effective stdio contract, bounded audit, frozen installation, and matched daemon attestation pass. | Run controlled native behavior on both hosts, then add a safe procedure-correction lifecycle. |
| External authoritative references; no native connector broker | **Proven for current scope** | Pilot documentation references official Codex and Claude memory behavior; no connector broker was added. |

## Completion criteria verdict

The goal is **not complete**.

### 2026-09-05 completion-criterion reconciliation

Three independent read-only audits reconciled the broad completion wording against current source
and recorded evidence:

- The explicit secret criterion is **proven for the preregistered synthetic canary classes**, using
  the real-RocksDB memory, procedure-command, and current-plan write paths. This is intentionally
  not a universal DLP claim; unknown formats and upstream host/connector handling remain outside it.
- Confirmed deletion remains proven for the bounded Engram-owned sequential writer model after
  adding correction proposals. Pair deletion/unlock and a content-free receipt with exact affected
  IDs are one transaction. Telemetry, commits, and free-form references are then purged
  idempotently; an injected post-delete failure survives a fresh service and resumes from the
  receipt, which is deleted only after success. Retry projection counts are honestly marked
  incomplete. Supported commit actions mint immutable IDs, while concurrent raw datastore commit
  rewrites, concurrent new cross-item references, external copies, backups, and host transcripts
  remain outside Engram's bounded deletion claim.
- Correction is **partial**. Candidate contract v4 gives the selected restricted agent profile a
  proposal-only route and reserves durable inspection/application for the full profile. The exact
  pair transition is digest-bound, transactional, restart-recoverable, and creates no manual-review
  authority. The scope is a caller-selected match, not identity authentication; same-UID full-profile
  access and semantic conflicting adds/deletion are outside the enforcement claim. Procedure
  correction is unsupported. The exact audited candidate is now installed with matched daemon and
  host configuration attestations, but neither native host has exercised the proposal/apply/fresh-
  retrieval journey yet.
- Engram process-boundary persistence and a limited fresh-Codex-task retrieval are proven, but no
  evidence crosses an actual Claude compaction or a controlled Codex session/process boundary with
  transcript-only confounds excluded. No frozen bundle has been replicated on a genuinely separate
  machine or VM. Local isolated homes and the reporter's cross-host label must not be described as
  independent-environment portability.

An already-running Colima VM provides a concrete next replication target without relabeling current
evidence: Ubuntu 24.04.4, Linux 6.8.0-100-generic, aarch64, under macOS
Virtualization.Framework. The repository is mounted there, but Rust and the agent hosts are not
installed, so this is readiness evidence only. A valid replication must build or transfer an
attested Linux bundle into VM-owned state and record the environment ID, OS/architecture, state
source digest, and executable hashes.

These classifications now incorporate schema 15's completed positive repeated outcome. They remove
two stale broad gaps (bounded secret canaries and Engram-owned deletion), replace the old
correction-unavailable finding with the narrower intent/adoption/native-validation boundary, and
leave native stale/expiry, actual host-boundary continuity, and independent-environment replication
as the material completion gaps.

### 2026-08-31 deterministic identity-boundary addendum

The product-side boundary requested by the final batched-pilot diagnosis is now implemented and
provider-free verified. Both lean `orient` and `memory(action=procedure_match)` return one compact
`identity` object that separates checkout identity from project authorization. Repository name,
credential-free normalized remote, checkout root, record correlation, and tracked component
path/hash can be complete while `identity.project.status=requires_confirmation` and the project
name remains null. Exact caller input, validated task scope, or a correlated repository/component
project link is required before status becomes `authorized`; a repository basename never grants
project scope.

The no-result harness route now consumes this result rather than asking Codex or Claude to rebuild
identity through three prose-directed reads. An empty verified-procedure result permits at most one
bounded operation-specific tracked lookup from `identity.repository.checkout_root`, forbids
broadening or candidate execution, and requires a user question for project confirmation. This is
the intended low-turn shape, not another retrieval/ranking redesign.

Evidence is provider-free but broad: all 271 runnable `engram-index` tests and its doc test pass,
all 11 repository integration tests pass, the moved-checkout procedure regression passes, 46 of 48
memory integration tests pass, strict library Clippy passes, formatting passes, the repository
`target/debug` remains absent, and the new unlinked lean fixture remains below 8,192 bytes. The two
remaining memory tests were prevented from creating RocksDB fixtures solely by the disk-reserve
gate (16,012,677,120 available bytes versus 19,893,251,686 required), not by test assertions.
The same gate rejected an Engram handoff dry run before any write. The operator later authorized
cleanup of that exact 12.8-GiB generated target; repository `target/debug` remained absent, the
daemon recovered, and a compact durable Engram handoff was stored without changing live adapters.

This upgrades deterministic identity and lean progressive orientation from “missing product
boundary” to “implemented and provider-free verified.” It does not upgrade either to native-host
proof: the completed 0/6 pilot remains immutable and no provider lane was replayed.

The discriminating evaluator slice is now also implemented provider-free as forward-only schema
12. It accepts explicit structured project ambiguity for the intentionally unlinked Orbit fixture,
requires exact correlated `orient` and `procedure_match` identities, independently verifies the
live checkout/remote and tracked component hash, rejects model-only identity claims and host
identity rereads, and freezes exact structured output plus unchanged packet/token/duration gates.
The tracked Orbit fixture now names component `queue-worker`. Five schema-12 discriminating tests
and all 148 evaluator tests pass; strict Clippy and formatting pass. The preregistered six-arm,
one-repetition file-cache protocol SHA-256 is
`d3e1bfa99874c433310f1807f611481919a1b1bfca692462da1fba4bf889c4d7`.

This does not yet add native-host evidence. The successful external build initially left only
19,548,086,272 bytes free against the mandatory 19,893,251,686-byte reserve, so preparation stopped
provider-free with a 345,165,414-byte deficit. The operator subsequently authorized deletion of
that exact generated target; Cargo recovered 15.3 GiB without rebuilding or touching any completed
pilot. The preserved binaries then produced a fresh, self-contained schema-12 successor at
`/Users/yuval.meiri/.engram/evals/native-memory-pilot/structured-identity-no-result-forward-file-cache-bounded-20260901-01`,
run-plan SHA-256
`52c366344844421a58d1245881c686b9b5663e41a48b8f0c3800eaccd0f9feae`.
After an approved cleanup restored the reserve, live Codex independently reported Engram enabled
over stdio and the installed server completed a six-tool restricted-profile MCP handshake; no live
setting or binary changed. The approved provisioner created exactly three owner-only isolated
Codex cache copies, and all three lanes became ready. Fresh preflight checks remained pristine.
Teaching then ran exactly once across all six lanes with six zero exits, no recovery or replay,
135,778 micro-USD of Claude spend, `invalid=false`, and zero audit failures. The three Codex lanes
subsequently passed the exact one-hour retention boundary. After fresh pristine gates, activation
ran exactly once for Codex lanes 2, 4, and 6, followed by exactly one evaluation for every lane.
All nine new provider processes exited 0, no trace was recovered or replayed, and the final audit
is `complete=true`, `invalid=false`, with zero integrity failures.

The completed result is intentionally not promoted into a success claim. All six lanes safely
abstained, ran no repository command, applied no Atlas context, attempted no native-memory write,
and recorded zero repeated failures. Zero of six passed the full contract because no lane read the
tracked `runbooks/deploy-worker.md` or observed `ORBIT_ONLY_CANARY`. Only Claude lean Engram
preserved exact identity. Both Codex treatments reread component identity instead of operation
evidence, while Claude combined changed `procedure_match` from the component cwd to the repository
root and lost component identity. All resource budgets passed; Claude lean Engram alone
descriptively Pareto-dominated native memory. The strict result is therefore `partial` with
`portable_incremental_value_observed=false`.

The machine report SHA-256 is
`6897557e6ddf49c7c67eae5e4a9ef818eb3243c9395751907a8ed9006ed61c8b`. Exact traces, costs, and
the root-cause record are in
`native_memory_pilot_v1/SCHEMA12_STRUCTURED_IDENTITY_RESULTS_2026-09-02.md`. The next product slice
must return one bounded deterministic operation-evidence path when uniquely resolvable and must
bind or preserve the original cwd across `orient` and `procedure_match`; another prompt-only
successor is not justified.

That forward source slice is now implemented and provider-free verified. A true no-candidate
`procedure_match` may return one Git-tracked checkout-relative runbook path plus SHA-256 and ranking
reason, but no contents; ties, unsafe/untracked/symlinked/oversized sources, unresolved conditions,
and known rejected candidates abstain without a suggestion. Codex and Claude adapter source copies
the exact cwd returned by `orient` and permits one exact operation-evidence read without identity
rereads, project invention, broadening, or candidate execution. The full `engram-index` library
suite passed (273 passed, one model-download test ignored), strict all-target Clippy and formatting
passed, and the schema-12 regression keeps the serialized call at or below 8,192 bytes without the
canary body. A source-built in-memory MCP smoke through Codex 0.152.1 then returned exactly
`runbooks/deploy-worker.md`; a second trace read it once, observed `ORBIT_ONLY_CANARY`, preserved
null project authorization, and did not reread component identity. The model misreported its own
command count as zero despite the trace's one read, reinforcing trace-derived scoring. Exact hashes
and smoke evidence are recorded in the schema-12 result document.

No completed lane was replayed and the dirty source-built executable was not installed. The live
Codex integration nevertheless remains independently ready: its enabled stdio server resolves to
the installed 0.2.3 executable, all six required MCP tools were observed, and a fresh ephemeral
Codex task successfully called lean `orient` and local `procedure_match`. Native cross-host proof of
the forward source slice still requires a new controlled successor.
The flagship completion verdict therefore remains **not complete**.

### 2026-09-04 Claude required host-action diagnostic

A forward-only source candidate now makes the no-result operation-evidence read an explicit
machine-readable host-action contract. The result carries
`required_before_final_abstention=true`,
`allowed_when_project_requires_confirmation=true`, and
`authorizes_procedure_execution=false`; generated Codex/Claude guidance defines the read as the
next required host action rather than optional prose.

One fresh isolated Claude Code 2.1.260 diagnostic called `procedure_match` once, read the exact
absolute returned path once, observed the attested Orbit marker, and safely abstained without a
procedure command, wrong-scope Atlas context, network access, or native-memory write. Source tests,
the complete `engram-index` and `engram-cli` suites, strict Clippy, formatting, and diff checks pass.
No live adapter, setting, or binary changed, and no completed pilot lane was replayed.

The first diagnostic is deliberately not promoted to a product claim: it is one non-preregistered
repetition, and Claude shortened and rewrote the candidate's required full exact
`procedure_match.query`. Inspection found that verbatim full-prompt copying was not an evaluator
gate, was not mechanically enforceable through model-facing prose, and made the echoed retrieval
packet unbounded. The forward contract now accepts only a task-focused query of at most 512
characters, preserves operation nouns/identifiers/failure text, rejects oversized input, and
keeps authorization in exact cwd plus structured local scope. A second isolated Claude diagnostic
used a 27-character worker-procedure query and repeated the exact source read plus safe abstention.
All affected suites and static checks pass. Repeated multi-case cross-host validation remains open.
Evidence is in
`native_memory_pilot_v1/CLAUDE_REQUIRED_HOST_ACTION_SMOKE_2026-09-04.md` and
the two `native_memory_pilot_v1/claude_*_smoke_2026-09-04.json` records. The flagship verdict
remains **not complete**.

The source repair is now preregistered forward-only as schema 15. Its evaluator makes bounded query
term preservation and the required ambiguity-safe, non-authorizing host-action fields hard
acceptance gates. The fresh six-arm matrix uses three repetitions (18 lanes), exact resource
limits, dedicated runtime copies, and no inherited or replayed provider artifacts. Re-attestation
passes. A pre-execution adversarial check retired the first provider-free preparation because a
missing false-valued authorization field could pass; the replacement evaluator requires every
boolean explicitly and adds missing-field coverage. Its audit reports `invalid=false`, zero
failures, all lanes `prepared`, and no outputs. Provider execution has not started because the nine
isolated Codex file-cache homes remain unauthenticated. This preparation advances the proof plan
but is not outcome evidence and does not change the **not complete** verdict. See
`native_memory_pilot_v1/SCHEMA15_REQUIRED_HOST_ACTION_BOUNDED_QUERY_REPEATED_PREPARED_2026-09-04.md`.

- Candidate-level ambiguity, scope safety, packet bounds, secret canaries, correction/deletion, and
  verified procedure applicability have strong provider-free evidence.
- The installed CLI, adapter files, lifecycle settings, and host launch shapes match the 0.2.3
  candidate. The topology-repair plan completed teaching, retention, Codex activation, and all six
  provider evaluations without replay. Its final audit is complete and valid.
- The topology-repair result strengthens the directional evidence: all four Engram and combined
  lanes passed the full task, both native-only controls failed identity, and every treatment pair
  Pareto-dominates native memory on the preregistered metrics on both hosts. This is one case with
  one repetition, however. The completed repeated result below closes reliability for its two
  applicability cases; broader safety cases, host-visible packet bounds, and compaction/resume
  remain unproven natively. The topology result supports the direction but does not itself complete
  the broader flagship journey.
- The completed schema-6 prerequisite-mismatch case is a valid negative counterweight. All six
  lanes abstained safely, but no lane completed canonical identity, exact first-action reporting,
  and a correlated current-condition read. Only Claude combined improved over its native control;
  Codex treatments tied native and carried substantial token overhead. This result motivated the
  forward harness/evidence repair below.
- The forward repair rerun is also complete and valid, and improves the diagnosis rather than the
  product claim. Scoped identity now improves both Codex treatments and Claude lean Engram, but no
  lane performs the mandatory prerequisite read or reports the exact first action. Both Claude
  treatments also lose procedure revalidation relative to Claude native. The reporter observes only
  a partial Codex-side signal and explicitly rejects portable incremental value. The next slice
  should make prerequisite inspection a deterministic host-boundary action, not another retrieval
  or ranking change.
- Schema 8 completes that host-boundary slice. Both Codex Engram treatments pass the full mismatch
  contract and Pareto-dominate Codex native memory. Claude lean also made the exact correlated
  `procedure_match` call, received the required value-redacted source observation, and abstained;
  however, the frozen evaluator required an explicit `is_error: false` that real successful Claude
  MCP results omit. The official frozen report remains a partial signal and rejects portability. A
  forward-only parser fix passes all evaluator checks and makes the immutable Claude lean trace
  pass in a post-hoc diagnostic, yielding `engram_across_hosts`. This is strong directional evidence,
  not a preregistered cross-host claim.
- The corrected-parser source matrix is now complete and valid. Codex lean Engram and combined
  memory each pass both the matched execution and mismatched abstention cases and Pareto-dominate
  Codex native memory. Claude native gets both task outcomes right, while each Claude Engram
  treatment gets only the mismatch outcome right and never revalidates the procedure. The matched
  Claude treatments stop after orientation; one mismatch treatment calls `procedure_match` first
  without its required query and then retries with an unrelated query. The valid result is therefore
  `partial`, and portable incremental value remains unproven. The next slice is a concise,
  provider-free Claude adapter/orientation routing repair followed by a new discriminating host
  run—not replay of this matrix or immediate expansion to the frozen three-repetition plan.
- The schema-11 Claude-only discriminating run is also complete and valid, and it rejects the first
  routing repair as insufficient. The ambiguity exception is present and understood, but the route
  still depends on literal learned/remembered phrasing. Both matched Engram treatments stop after
  orientation, one mismatch treatment lists memory instead of matching a procedure, and neither
  treatment Pareto-dominates Claude native. The report signal is `not_observed`; independently, a
  single-host diagnostic cannot establish portability. The next repair must remove this host-side
  intent-classification branch by requiring one bounded repository-local `procedure_match` for
  every actionable repository task and prohibiting `memory(list)` as a substitute before another
  fresh, never-replayed diagnostic.
- The fresh unconditional-route successor first validated lean Engram on Claude Code. Its repeated
  cross-host successor now closes that specific uncertainty. After attested exact-prefix teaching
  recovery and the one-hour Codex retention gate, activation and evaluation each ran exactly once;
  all 54 new provider processes exited 0 and no trace was recovered or replayed. Lean Engram and
  combined memory each passed all 12 full contracts across Codex, Claude Code, matched execution,
  prerequisite mismatch, and three repetitions. Native passed zero of 12. Every treatment/native
  matched-pair comparison Pareto-dominates native, so the strict frozen reporter exits 0 with
  `engram_and_combined_across_hosts`. This is the strongest current evidence for the product
  direction, but it remains one machine, one repository family, two cases, isolated adapters, and
  no preregistered overhead bound. Ambiguity/task identity, missing/stale source,
  compaction/resume, correction/deletion, secrets, live harnesses, Cursor, and independent
  environments still prevent full flagship completion.
- The repeated schema-7 wrong-scope result now proves the narrower cross-project safety boundary:
  every lane abstained, no Atlas command ran, and Engram returned no applicable Atlas procedure.
  It also rejects the current no-result journey. Zero of 18 lanes proved Orbit identity and local
  applicability, all treatment/native deltas are zero, and the strict signal is `not_observed`.
  The next slice must make safe no-result non-terminal and close authoritative local evidence from
  `current_checkout_root`; it should not change ranking or weaken scope abstention.
- The forward no-result bounded successor is now complete and valid. Plan
  `/Users/yuval.meiri/.engram/evals/native-memory-pilot/no-result-evidence-closure-forward-file-cache-runtime-complete-bounded-20260831-01/run/run-plan.json`,
  SHA-256 `9e864c2b3e29c42afbf66509824697b7c5a01d4798e9c7bb0c3c43cefebba5ac`,
  completed teaching, the exact one-hour retention gate, all three Codex activations, and all six
  evaluations exactly once with no recovery or replay. The final audit is `complete=true` and
  `invalid=false`. Every lane abstains safely, attempts no procedure command, leaks no Atlas
  context, and has zero repeated failures, but 0/6 pass the frozen identity/first-action/evidence
  contract. All packet limits pass; Claude lean and Codex lean violate the token bound, while Codex
  combined violates token and duration bounds. The strict reporter returns `not_observed`; no
  treatment Pareto-dominates native. Trace review also proves one frozen Claude parser false
  negative and an Engram-lane identity-audit mismatch. Both are repaired forward only with 143
  passing tests, while the immutable outcome stays unchanged. Forward adapters now require all
  three evidence requirements while batching independent host work to reduce model-turn overhead.
  The fresh self-contained batched successor passed attestation, received exactly three explicitly
  authorized protected caches, and completed teaching, retention, activation, and evaluation
  exactly once with no recovery or replay. Its final audit is `complete=true`, `invalid=false`, but
  0/6 lanes pass exact identity acceptance. All six abstain safely with no command, wrong-scope
  context, repeated failure, or native-memory write attempt. Claude's Engram arms improve
  first-action/evidence metrics over native, but all four treatment pairs exceed the token budget,
  both Codex pairs exceed duration, and portable incremental value is false. This closes the
  prompt-strengthening hypothesis negatively: another instruction-only successor is not justified.
  The next slice should return canonical local identity and evidence correlation through one
  deterministic bounded structured boundary, then retest without replay. The flagship goal remains
  incomplete.

The operator approved the exact $0.50 Claude aggregate ceiling, Codex quota, installation,
adapter/settings updates, daemon restart, and redesign of the unavailable access-token
prerequisite. Installation adoption is complete. The pilot requires isolated, Keychain-backed
ChatGPT browser login and no secret environment variable. The first Keychain-backed plan
re-attested and all three Codex lanes became ready. Its approved teaching phase then stopped on the
first Claude lane: `dontAsk` denied required tools, and the first request reported `$0.0918771`,
overshooting its `$0.083` allocation. No Codex or later Claude lane ran.

That replacement passed login and approval, but its first Claude lane used two turns recovering
from incomplete Engram write arguments and stopped at `$0.0524307` against its `$0.050` allocation.
It correctly observed the frozen failure and successful command; no later lane ran. The evaluator
then froze a complete exact write object into Engram teaching prompts, and the required write-field
contract was added to the production Codex/Claude adapter generator and installed project
adapters. Candidate, installed binary, and restarted daemon attest as matched.

The approved adapter-contract plan's first Claude lane completed in four turns for `$0.0425264`.
It observed both commands, submitted the exact Engram record, and the trusted evaluator activated
candidate `019fe717-2fed-7cc1-af40-3a7c810e7f8d` against the frozen receipt. Claude Code produced no
auto-memory artifact. That is a legitimate result for this case—Claude's native policy may decline
to save agent-discovered code/tool facts—so treating absence as an integrity failure made the
benchmark incapable of measuring the native product boundary.

Protocol schema 4 records native artifacts as `observed: true/false`, permits evaluation after clean
native non-capture, keeps Engram state and exact verification required, rejects stale,
pre-existing, symlinked, shell-manufactured, or substantive premature artifacts, and gives Claude
`Write`/`Edit` only inside its isolated auto-memory directory. The plan records 186,836 micro-USD of
predecessor spend, an approved `$0.30` allocation, and the existing `$0.50` ceiling; its immutable
digest is `35e52d187bd8ea0933d47a27318de9e82c81565806c4579456653ac86c172ffa` at
`/private/tmp/engram-native-memory-pilot-v1-native-outcome-final.hZfOgq/run-plan.json`.

All isolated Codex homes now use Keychain-backed ChatGPT login. Teaching completed across all six
lanes with no audit failures. Claude costs were `$0.0534471` (combined, accepted only under the exact
documented end-of-turn budget envelope), `$0.0471575` (lean Engram), and `$0.0313856` (native-only),
for `$0.1319902` in this plan's teaching phase. Claude native-only generated a non-empty
`MEMORY.md` containing the cobalt procedure, while Claude combined generated no native artifact;
both are valid measured outcomes. Codex native and combined lanes created only attested-host empty
scaffolds (`No raw memories yet`). After the final one-hour eligibility time at
2026-08-09 20:33:30 +03:00, digest, executable, runtime-contract, Keychain-authentication, and
lifecycle checks remained pristine. All three Codex activation calls exited 0, the empty scaffolds
remained a valid `observed: false` outcome, and all six lanes became `ready_for_evaluation`.

The approved source evaluation then stopped on lane 1 before a model result because Claude Code
2.1.226 rejected the optional draft-2020-12 `$schema` declaration locally. Its evaluation trace is
an immutable zero-byte file, stderr contains the exact validator diagnostic, no evaluation report
or provider cost exists, and the source plan is correctly `invalid`. No source artifact was
repaired or replayed.

The evaluator now uses a portable common schema subset and can prepare only an evaluation-only
successor for that exact failure. Before execution, the ChatGPT app auto-updated and replaced the
attested Codex binary. The runner stopped provider-free. A signed older ChatGPT release supplied the
exact original Codex 0.147.0-alpha.1.2 bytes, and a second recovery successor recorded the relocated
path while requiring the exact source version and SHA-256. No teaching or activation evidence was
copied, changed, or replayed.

The completed successor is
`/private/tmp/engram-native-memory-pilot-v1-evaluation-recovery-relocated.Fgi1lm/run-plan.json`,
SHA-256 `0298a4ebde273183c07f5badcaa8009081cc39a2ef0297053760ad08e7ece49b`.
Its first Claude call produced one completed schema-valid `StructuredOutput` and then hit the exact
budget envelope with `stop_reason=tool_use`. A narrow runner correction recovered only that unique
output from the immutable trace and never replayed the call. The other five lanes then ran once.
The final evaluation report SHA-256 is
`0fa5760678a1c278fa2cddb9c6d8af7aa40215bee1b2ba26169ada0d836ba9b2`.

All six lanes are `evaluation_complete`; `invalid=false`. No lane passed. All six avoided the known
amber failure, but the Engram lanes exposed repository/project-link, soft skill-trigger, and local
prerequisite-resolution failures. Claude native read its generated memory and successfully ran the
cobalt command, but failed the frozen canonical identity, evidence, first-action, and exact-command
reporting contract. The matched report is `not_observed`, with no treatment Pareto-dominating native
memory on either host. Claude evaluation spend was 132,882 micro-USD; total accounted Claude spend
was 451,710 micro-USD under the existing 500,000 micro-USD ceiling. Full evidence and the next
boundary-repair slice are recorded in `native_memory_pilot_v1/RESULTS_2026-08-10.md`.
