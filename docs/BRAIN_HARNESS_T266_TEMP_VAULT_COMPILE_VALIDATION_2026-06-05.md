# Brain Harness T266 Temp Vault Compile Validation

Date: 2026-06-05
Status: completed isolated temp-path validation. Canonical vault remains uninitialized.

## Scope

This slice validates that current persisted Engram Memory OS data can compile into the generated
Markdown vault projection without touching the canonical vault.

It writes only to:

```text
/private/tmp/engram-t266-vault-smoke-20260605
```

It does not initialize or compile `/Users/yuval.meiri/.engram/vault`, run M6 migration or
quarantine actions, edit review pages, mutate lifecycle state, archive memory, delete data, change
ranking or `orient`, change public MCP/schema/storage/index/document-index behavior, run native
Claude, edit harness files, push branches, or touch user-owned files.

## Research Question

Can Engram validate the current-data vault compile path without making the durable canonical vault
live?

## Hypotheses

| Type | Hypothesis | Result |
| --- | --- | --- |
| Preferred | A unique temp-path `vault init` plus `vault compile` will generate the expected current-data projection and leave the canonical vault absent. | Supported. |
| Null | Current Memory OS data no longer compiles into the generated vault projection, or compile writes outside the temp path. | Not supported. |
| Simpler alternative | Keep the vault gate entirely missing until the canonical path is approved. | Rejected because temp compile gives useful compileability evidence without durable vault writes. |
| Failure | The result is overstated as production vault completion, or hidden writes touch canonical vault/M6/lifecycle state. | Avoided. |

## Consultation

AI Council recall found no strong vault-specific prior decision. A fresh three-model broadcast
agreed the temp-only compile is safe and useful if the report says only that compilation capability
was validated, not that the canonical vault is complete. The Council recommended hard stops for any
write outside `/private/tmp`, source-data mutation, schema migration, major count mismatch, or
canonical-path coupling.

Claude Bridge read-only critique timed out after 120 seconds and is treated as a consultation
confound, not supporting evidence.

## Execution

Preflight canonical status for `/Users/yuval.meiri/.engram/vault`:

| Field | Value |
| --- | ---: |
| Exists | false |
| Initialized | false |
| Total files | 0 |
| Generated files | 0 |
| User files | 0 |
| Expected generated files | 2245 |

Preflight temp status for `/private/tmp/engram-t266-vault-smoke-20260605`:

| Field | Value |
| --- | ---: |
| Exists | false |
| Initialized | false |
| Expected generated files | 2245 |

Operations run through the Engram MCP vault tool:

```text
vault(action="init", vault_path="/private/tmp/engram-t266-vault-smoke-20260605")
vault(action="compile", vault_path="/private/tmp/engram-t266-vault-smoke-20260605")
```

`init` created the expected skeleton directories:

- `99_System`
- `memory`
- `memory/items`
- `memory/commits`
- `entities`
- `projects`
- `repositories`

## Results

Post-compile temp status:

| Field | Value |
| --- | ---: |
| Exists | true |
| Initialized | true |
| Total files | 2245 |
| Generated files | 2245 |
| User files | 0 |
| Memory items | 1585 |
| Knowledge commits | 536 |
| Repositories | 9 |
| Entities | 32 |
| Projects | 79 |
| Expected generated files | 2245 |

Post-compile canonical status for `/Users/yuval.meiri/.engram/vault` remained:

| Field | Value |
| --- | ---: |
| Exists | false |
| Initialized | false |
| Total files | 0 |
| Generated files | 0 |
| User files | 0 |
| Expected generated files | 2245 |

The temp output uses 9.8 MB and was left in `/private/tmp` as inspection evidence.

## Page Sampling

Sampled pages:

- `99_System/Vault-Index.md`
- `memory/items/019e964e-f498-7c10-bf67-929a2d0ab6f9-current-plan-after-t265-runtime-refresh-documentation.md`
- `projects/engram/index.md`

All three sampled pages were found, marked as generated by the vault tool, and contained
frontmatter plus the Engram generated marker:

```text
<!-- engram:generated:file memory-vault-v1 -->
```

The current-plan sample included `page_type: "memory_item"` and
`engram_id: "019e964e-f498-7c10-bf67-929a2d0ab6f9"`. The project sample included
`page_type: "project"` and `project_name: "engram"`. The vault index included the current summary
counts.

Direct marker/frontmatter scans over the temp vault returned no files missing the generated marker
and no files missing frontmatter delimiters.

## Lint

`lint(action="run", vault_path="/private/tmp/engram-t266-vault-smoke-20260605", limit=20)` returned
the known global lifecycle/feedback findings and applied zero safe actions. No vault-page metadata
finding appeared in the returned set. This is supporting evidence only; the direct marker and
frontmatter scans are the vault-specific validation.

## Decision

T266 validates the generated vault compile path for current data in an isolated temp directory.
It does not initialize the durable canonical vault and does not complete the user-facing vault
gate. The canonical vault at `/Users/yuval.meiri/.engram/vault` remains absent and requires a
separate explicit durable-vault decision before it is initialized or compiled.

Completion wording after T266:

- Validated: current Memory OS data compiles into a generated vault projection in isolated temp
  output.
- Not validated: canonical vault initialization, canonical vault update policy, durable user-facing
  vault readiness, M6 apply, lifecycle cleanup, migration completion, native Claude parity, or
  remote publication.
